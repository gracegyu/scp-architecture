# otp-mail-api 명세 — 리뷰 코멘트 추적 (es-infra PR 13416)

> **작업용 문서**. 각 항목=PR 코멘트 1건. 게시된 항목은 `Comment` 블록(게시본 원문)에 `[thread NNNNN]`·게시 기록·Jack 회신 원문을 함께 둔다. `Comment 초안` 블록인 항목은 **미게시**(사용자 선별에서 제외)다.
> 답변 스타일: 서술문(개조식 지양) · 대외 이모지 없음 · 내부 라벨(C-NN) 노출 금지 · `#숫자` 오링크 회피.

- **PR**: https://dev.azure.com/ewoosoft/platforms/_git/es-infra/pullrequest/13416
- **대상 문서**: `docs/otp-mail-api-spec.md` — 브랜치 `feat/devops-215-otp-mail-api`
- **헤드 이력**: `8cc640d`(R1 착수) → `f384cc8`(알람 배선, R1 작성 중 유입) → `67dec10`(R2 대상, 문서 267→450줄)
- **리뷰 중 갱신**: 작성 도중 `f384cc8`(CloudWatch 알람 모듈 신설 + OTP API 알람 배선)이 push되어 스펙 4장 로그 절이 갱신됨. 아래 줄 번호는 `f384cc8` 기준이며, 해당 커밋이 해소한 항목은 본문에 반영했다.
- **주제**: CleverRC / Clever Lab / Clever Dent Manager 공용 이메일 OTP 모듈 (DEVOPS-215 · 원 요청 EVNCRC-3380)
- **작성자**: 임건혁(Jack) · **리뷰어**: 우리
- **대조 자료**: 같은 PR의 `terraform/envs/prod-us-east-1/apps/lambda/otp-mail-api/**`(core·adapters·entrypoints·openapi) · `apps/otp_mail_api.tf` · `platform/otp_gateway.tf`
- **리뷰 기준**: `spec-standard.md`(SRS 고정 구조 1~9장) · `spec-philosophy.md`(상세도 Case 판정) · `abc-spec-review` 체크리스트 A~M
- **상태(R1)**: 36건 도출(그중 1건은 `f384cc8`로 대부분 해소) → 사용자 선별 후 **총평 1건 + 항목 26건 게시**(2026-09-01T22:52~23:08, 전규현 명의, thread 87977~88003 · 전부 Active) · **10건 선별 제외**(C-17·C-21·C-22·C-26·C-31·C-32·C-33·C-34·C-35·C-36)
- **상태(R2, 2026-09-02)**: Jack 반영본(`67dec10`) 대조 → R1 게시 27건 중 **21건을 우리가 Resolve**, 나머지 4건은 Jack이 직접 close, **2건(thread 87983·87984) Active 유지**. 신규 10건(C-37~C-46) 도출 → 사용자 선별로 **7건 게시**(thread 88235·88237·88238·88239·88240·88241·88243, 2026-09-02 04:46~04:49) · **3건 제외**(C-40·C-43·C-45).
- **상태(R3, 2026-09-03)**: Jack 반영본(`4f0d868`) 대조 → 열린 8건 **전건 Resolve**. 신규 지적 1건(C-47) 도출·미게시.
- **종료**: 2026-09-03 리뷰 종료 · **열린 스레드 0건** · 승인은 전규현이 직접 투표.
- **이월(머지 후 추적 대상)**: 발송 총량 상한 **D-02**(재검토=첫 온보딩 직후 실측 시점, SES 이벤트 구독과 묶음) · 코드 저장 페퍼 **D-10**(재검토=다음 코드 반영 배치) · 바운스 수신자 귀속(SES 이벤트 구독, `monitoring.tf` 주석에 근거) · 미게시 13건은 본문에 초안째 보관.
- **정정 이력**: 게시된 총평이 알람 커밋 관련 "남는 부분만 해당 자리에 따로 적었습니다"라고 했으나 그 자리(C-32)가 선별에서 빠져 가리킬 곳이 없었다. 삭제 후 재게시 대신 **thread 87977 · comment 1을 제자리 PATCH**(2026-09-01T23:17)해 C-32 내용을 총평 문단 안으로 흡수. 원문은 `scratchpad/old-content.txt`에 보관.

---

## 리뷰 기준 판정

### 상세도 Case (spec-philosophy §3.3 5개 축)

| 축 | 판정 | 근거 |
| --- | --- | --- |
| 프로젝트 난이도 | 상세히 | 인증 경로 신규 도메인. 실패 시 전 제품 로그인 차단 |
| 커뮤니케이션 용이성 | 상세히 | 코드 소유가 앱 레포로 이관 예정(미결 4번). 작성자와 구현·운영 주체가 갈림 |
| 개발팀 경험 | 중간 | 사내 유사 모듈 없음 |
| 참고 제품 유무 | 상세히 | 사내 공용 OTP 모듈 선례 없음 |
| 외주·분산 여부 | 상세히 | 소비 주체가 베트남·이집트·브라질·호주 제품팀으로 분산 |

**판정: Case C(고상세).** spec-philosophy §3.4의 "외부 연동" 기본값과도 일치하고, §3.6이 지정한 *상세도 부족이 더 위험한 항목* 다섯 가지 중 네 가지(외부 인터페이스·비기능 요구 수치·법 제약·에러/부작용)에 직접 걸린다.

### 문서 유형 판정

SRS도 One Pager도 아니다. 실질은 **API 레퍼런스 + 설계 노트**다. 그 층위에서는 밀도가 높지만, 스펙이 담아야 할 층(목적·범위·비목표·사용자 계층·가정과 종속·성능·비기능·제약·확장성·결정 근거)이 대부분 비어 있다. 아래 A절이 그 지적이다.

### 심각도 요약

| 심각도 | R1 | R2 | 성격 |
| --- | --- | --- | --- |
| 🔴 Critical | 10 | 1 | 베이스라인 전 채워야 할 것 — 결정이 없거나, 문서끼리 충돌하거나, 지금 정하지 않으면 나중에 구조를 바꿔야 하는 것 |
| 🟡 Warning | 23 | 7 | 계약 모호·운영 공백·장기 지속성 리스크 |
| 🟢 Info | 3 | 2 | 서술 보강 |

**게시 현황**: 총 46건 중 **33건 게시**(R1 26 + 총평 + R2 7) · **13건 미게시**(선별 제외).

| 절 | 범위 | 항목 |
| --- | --- | --- |
| A | 스펙으로서의 기본 구조 | C-01 ~ C-09 |
| B | 제품 결정 — 검증 흐름 | C-10 ~ C-11 |
| C | 확장성(5~10년) | C-12 ~ C-17 |
| D | 보안 | C-18 ~ C-26 |
| E | 계약 정확성 | C-27 ~ C-31 |
| F | 운영·개인정보·감사 | C-32 ~ C-34 |
| G | 문서 정합 | C-35 ~ C-36 |
| R2 | 반영본 재검토 신규 | C-37 ~ C-46 |

---

## 총평 · [thread 87977] · **Resolved(fixed)**

- **Comment** (게시본 · 2026-09-01T23:17 수정 반영분)

```text
스펙 검토했습니다. 서술이 실제와 맞는지 확인하려고 같은 PR의 함수 코드와 테라폼도 함께 읽었고, 코드 품질 자체는 범위 밖으로 두었습니다.

먼저 좋았던 점입니다. 좁은 엔드포인트로 액세스 키 배포를 대체한다는 전제가 문서 전체에 일관되게 흐르고, 각 결정 옆에 왜 그렇게 했는지가 함께 적혀 있습니다. 저장을 발송보다 먼저 하는 순서, 소비 마킹을 조건부 쓰기 한 번으로 끝내 동시 검증에서 정확히 하나만 성공하게 만든 것, 만료 판정을 저장소 수명 관리에 맡기지 않고 코드가 직접 하는 것, 스토어별로 장애 방향을 다르게 잡은 것은 보통 빠뜨리는 항목입니다. 확장 요구가 오면 구현하지 말고 되물으라는 문단은 이런 종류의 서비스가 가장 자주 무너지는 지점을 정확히 막고 있습니다. API 레퍼런스로서는 이미 충분히 좋습니다.

의견은 크게 세 갈래입니다.

첫째, 이 문서를 앱 팀과 공유하는 스펙으로 쓰려면 지금 있는 층 위에 한 층이 더 필요해 보입니다. 지금 문서는 무엇을 어떻게 호출하는가는 정확한데, 이 모듈이 무엇을 달성하려는 것인지, 무엇을 하지 않기로 했는지, 누가 어디까지 책임지는지, 얼마나 빨라야 하고 얼마나 살아 있어야 하는지, 발송량이 얼마로 예상되는지가 없습니다. 이 항목들이 없으면 리뷰어와 구현자와 운영자가 각자 추정해서 일하게 되고, 특히 성능과 가용성은 숫자가 없으면 합격 불합격 판정 자체가 불가능합니다. 세 제품이 공유하는 인증 경로라서 이 부분이 더 중요하다고 봅니다.

둘째, 이메일 OTP에는 보통 두 가지 방식이 있습니다. 하나는 지금 스펙이 다루는 대로 사용자가 메일의 숫자를 화면에 입력하는 방식이고, 다른 하나는 메일 안의 링크를 눌러 검증되었습니다 화면을 보고 원래 화면이 다음 단계로 넘어가는 방식입니다. 두 번째 방식이 검토 대상에 없는 것 같은데, 문제는 지금 구조가 그 방식을 나중에 얹기 어렵게 되어 있다는 점입니다. 인증이 키와 소스 IP라서 사용자 브라우저는 이 API에 닿을 수 없고, 저장된 코드에 그 시도를 가리키는 식별자가 없어서 대기 중인 화면이 결과를 알 방법이 없으며, 메일 템플릿에 링크가 들어갈 자리도 없습니다. 지금 단계에서는 저장 키에 식별자 하나를 더하고 경로 하나를 여는 정도로 끝나지만, 나중에 요구가 오면 저장 구조와 인증 모델과 템플릿을 한꺼번에 바꿔야 합니다. 채택하지 않기로 하셨다면 그 판단을 비목표로 적어 두는 것만으로도 충분합니다.

셋째, 몇 년 뒤에 발목을 잡을 것들입니다. 문서가 테넌트 예시로 베트남과 이집트와 브라질과 호주를 드는데 언어는 한국어와 영어 둘뿐이고, 세 제품이 모두 같은 제목의 메일을 받습니다. 문구 파라미터를 막은 결정 자체는 옳다고 보지만, 그러면 인증된 테넌트를 보고 서버가 템플릿을 고르는 자리가 설계에 열려 있어야 하는데 지금은 그 이음매가 없습니다. 코드 슬롯이 수신자당 하나라 같은 사용자가 로그인과 이메일 변경을 동시에 진행하면 서로를 지우는 점, 자릿수와 유효시간이 전역 상수라 제품별로 다르게 갈 수 없는 점도 같은 성격입니다.

그 외에 보안과 계약과 운영에서 보이는 것들을 항목별로 나누어 아래에 코멘트로 남겼습니다. 가장 시급하다고 본 것은 발송 상한이 수신자별로만 걸려 있어 서로 다른 주소로 나가는 총량에는 제한이 없다는 점, 문서 조회 경로가 인증과 소스 IP 검사를 거치지 않아 첫 문장의 선언과 어긋난다는 점, 그리고 저장된 코드 해시가 여섯 자리 공간이라 사실상 평문과 같다는 점입니다.

리뷰를 쓰는 사이에 올려 주신 알람 커밋도 함께 보았습니다. 바운스를 핵심 위험으로 지목하면서 정작 감지 경로가 없다는 지적은 그걸로 해소됐습니다. 다만 그 위에 두 가지가 남아 보입니다. 알람 설명의 대응 절차가 바운스 발생 시 함수 로그에서 수신자와 테넌트를 찾아 억제하라고 되어 있는데, 함수 로그는 발송만 기록하고 바운스는 기록하지 않아서 그대로는 수행하기 어렵습니다. 발송 이벤트를 구독하고 있지 않아 어느 주소가 반송됐는지 알 방법이 없고, 계정 평판 지표는 무언가 잘못됐다까지만 알려 줍니다. 그리고 발송 실패에는 경보가 없습니다. 502를 만드는 경로는 코드가 예외를 잡아 정상 응답으로 바꾸기 때문에 함수 오류 지표에 잡히지 않아서, 메일이 계속 안 나가도 알아채지 못하는 상태입니다. 발송 이벤트 대상을 구독하시면 첫 번째가 풀리고, 발송 상한 쪽에서 말씀드린 총량 감시도 같이 얹을 수 있을 것 같습니다.

전체적으로 방향은 맞다고 보고, 위 항목들은 지금 시점에 채우면 비용이 거의 들지 않는 것들이라 정리 삼아 남깁니다.
```

- **게시** · 2026-09-01 22:52 전규현 명의 · 2026-09-01 23:17 제자리 수정(C-32 흡수) · 원문 사본 `scratchpad/old-content.txt`
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 리뷰 감사합니다. 지적을 두 갈래로 나눠 처리했습니다. (1) 문서로 해소되는 항목은 스펙에 반영했습니다 — 1장 앞에 개요(§0: 목적·기대효과, 범위·비목표, 사용자 계층·책임 경계, 검증 보증 범위, 외부 종속성, 성능·가용성 목표, 예상 발송량)를 신설하고, 결정 기록 표(§7)와 미결 표(§6, 담당/기한/티켓 열)를 추가했습니다. 커밋 efd9018. (2) 코드·인프라 동작 변경이 필요한 항목(발송 총량 상한, 코드 저장 페퍼, 코드 키의 용도 축, 502 시 레코드 삭제, 테넌트별 템플릿·정책 오버라이드)은 이미 apply된 리소스를 바꾸는 결정이라, 현재 동작을 정직하게 기술하고 §7 결정 기록(D-02/04/07/09/10)에 대안·기각 사유·다시 볼 조건과 함께 등재했습니다. 이 결정들은 별도로 받아 반영할 계획입니다. 각 스레드에 개별 회신 남깁니다. 또한 언어가 한국어/영어 둘뿐인 것은 아직 기획안이 확정되지 않았고 바텍 그룹사 내부 어드민에서만 사용하기에 우선 한국어/영어만 지원하도록 구현하였습니다.
- 조치: 문서 대개편(§0·§7 신설)으로 응답 · 상태: **Resolved(fixed)** (2026-09-02 Jack이 close) · 잔여 → C-46

---

# A. 스펙으로서의 기본 구조

## C-01 · docs/otp-mail-api-spec.md:1 · 🔴 · 문서 위상 — 요구사항 명세가 아니라 API 레퍼런스 · [thread 87978] · **Resolved(fixed)**

- **원문**
  > `# otp-mail-api 명세`
- **기준**: spec-standard.md SRS 고정 항목 구조 · abc-spec-review 체크리스트 A(항목 구조 완전성)
- **문제**: 제목은 명세인데 내용은 계약 표와 설계 노트다. 1장 Introduction(목적·범위·용어·대상 독자), 2장 Overall Description(제품 조망·사용자 계층·가정과 종속·단계별 요구사항·하위 호환성), 5장 Performance, 6장 Non-Functional(가용성·보안 요구·데이터베이스 요구·비즈니스 규칙·설계 제한·운영 요구) 중 어느 것도 대응하는 절이 없다. 3장 "왜 이렇게 좁은가"와 4장 "운영 규칙"이 그 일부를 대신하지만, 그 안에 담긴 것은 결정의 근거이지 요구사항이 아니다.
- **근거**: 이 문서는 앱 팀에 공유되는 계약 문서이고 코드 소유는 앱 레포로 넘어갈 예정이다(미결 4번). 작성자와 구현·운영 주체가 갈리는 구성이라 spec-philosophy §3.3 판정이 Case C(고상세)로 나온다. 지금 형태로는 "무엇을 어떻게 호출하는가"는 정확하지만 "무엇을 만족해야 하는가"가 없어 인수 판정 기준이 서지 않는다.
- **제안**: 전면 개편이 아니라 앞에 한 절을 붙이는 정도면 충분하다. 목적과 기대효과, 범위와 비목표, 사용자 계층과 책임 경계, 가정과 종속, 성능·가용성 목표, 예상 규모를 각각 한두 문단으로 추가하면 아래 C-02부터 C-09까지가 한꺼번에 해소된다.
- **Comment** (게시본)

```text
문서 전체에 대한 의견입니다. 제목은 명세인데 내용은 API 레퍼런스와 설계 노트에 가깝습니다. 무엇을 어떻게 호출하는가는 정확한데, 이 모듈이 무엇을 달성하려는 것인지, 무엇을 하지 않기로 했는지, 누가 어디까지 책임지는지, 얼마나 빨라야 하고 얼마나 살아 있어야 하는지, 발송량이 얼마로 예상되는지가 문서에 없습니다.

세 제품이 공유하는 인증 경로이고 코드 소유가 앱 레포로 넘어갈 예정이라, 작성하신 분과 구현하고 운영할 분이 갈립니다. 그런 구성에서는 이 층이 비어 있으면 리뷰어와 구현자와 운영자가 각자 추정해서 일하게 됩니다.

전면 개편을 말씀드리는 것은 아닙니다. 지금 1장 앞에 목적과 기대효과, 범위와 비목표, 사용자 계층과 책임 경계, 가정과 종속, 성능과 가용성 목표, 예상 발송 규모를 각각 한두 문단으로 붙이면 충분합니다. 아래에 개별 코멘트로 나누어 남긴 항목 대부분이 그 한 절로 함께 해소됩니다.
```

- **게시** · thread 87978 · 2026-09-01 22:53 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. 1장 앞에 §0 개요를 신설해 목적·기대효과(§0.1), 범위·비목표(§0.2), 사용자 계층·책임 경계(§0.3), 검증 보증 범위(§0.4), 가정·외부 종속성(§0.5), 성능·가용성 목표(§0.6), 예상 발송량(§0.7)을 각각 한두 문단으로 붙였습니다. 말씀대로 아래 개별 코멘트 상당수가 이 절로 함께 해소됩니다.
- 조치: §0 개요 신설(목적·범위·비목표·사용자 계층·종속성·성능·규모) · 상태: **Resolved(fixed)**

## C-02 · docs/otp-mail-api-spec.md:3 · 🔴 · 목적과 기대효과가 없다 · [thread 87979] · **Resolved(fixed)**

- **원문**
  > `CleverRC · Clever Lab · Clever Dent Manager 공용 **이메일 OTP 모듈**.`
- **기준**: spec-standard.md §1.1 Purpose · §1.2 Product Scope("비즈니스 배경 = 현재 문제점 + 목표 + 기대효과") · abc-spec-review 체크리스트 C
- **문제**: 무엇인지는 한 줄로 적혀 있지만 왜 필요한지가 없다. 3장은 "왜 이렇게 좁은가"를 답하는데 이건 *수단*의 근거이고, *목적*(어떤 문제를 풀려고 공용 OTP 모듈을 만드는가, 성공하면 무엇이 좋아지는가)은 문서 어디에도 없다.
- **근거**: 목적이 없으면 나중에 들어오는 요구가 범위 안인지 밖인지 판단할 기준이 없다. 3장의 하드 제약이 그 역할을 일부 하지만 "문구를 못 정한다"는 금지이지 판단 기준이 아니다.
- **제안**: 현재 문제(제품마다 OTP를 따로 만들고 있고, AWS 밖 제품에는 발송 수단을 줄 방법이 없다), 목표(세 제품이 공유하는 OTP 발급·검증 수단을 제공하고 자격증명 배포를 없앤다), 기대효과(중복 구현 제거, 발신 도메인 오남용 위험 차단)를 세 문장으로 적는다.
- **Comment** (게시본)

```text
문서 첫머리에 이 모듈이 무엇인지는 한 줄로 있는데 왜 필요한지가 없습니다. 3장이 왜 이렇게 좁은가를 답해 주기는 하지만 그건 수단의 근거이고, 어떤 문제를 풀려고 공용 OTP 모듈을 만드는지, 성공하면 무엇이 좋아지는지는 문서에 없습니다.

목적이 없으면 나중에 들어오는 요구가 범위 안인지 밖인지 판단할 기준이 서지 않습니다. 하드 제약 문단이 그 역할을 일부 하지만 그건 금지 목록이지 판단 기준은 아닙니다.

현재 문제와 목표와 기대효과를 세 문장 정도로 적어 주시면 좋겠습니다. 예를 들면 제품마다 OTP를 따로 만들고 있고 AWS 밖 제품에는 발송 수단을 줄 방법이 없다는 문제, 세 제품이 공유하는 발급과 검증 수단을 제공해 자격증명 배포를 없앤다는 목표, 중복 구현이 사라지고 발신 도메인 오남용 위험이 차단된다는 효과 정도입니다.
```

- **게시** · thread 87979 · 2026-09-01 22:53 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §0.1에 현재 문제(제품마다 OTP 중복 구현, AWS 밖 제품에 발송 수단 부재), 목표(세 제품 공유 발급·검증 수단으로 자격증명 배포 제거), 기대효과(중복 구현 제거 + 발신 도메인 오남용 인터페이스 수준 차단)를 세 문장으로 적었습니다.
- 조치: §0.1 목적·기대효과 3문장 추가 · 상태: **Resolved(fixed)**

## C-03 · docs/otp-mail-api-spec.md:150 · 🔴 · 비목표가 명시돼 있지 않다 · [thread 87980] · **Resolved(fixed)**

- **원문**
  > `> **하드 제약.** \`subject\` / \`body\` / \`from\` 파라미터는 만들지 않는다.`
- **기준**: spec-standard.md "비목표(Will Not Do) 표기 규약" — 1.2 마지막 문단 + 7.x 각 기능 끝
- **문제**: 하드 제약은 "이 파라미터를 만들지 않는다"는 인터페이스 금지이고, 비목표는 "이 서비스는 무엇을 하지 않는가"다. 후자가 없다. 실제로 이 서비스가 하지 않는데 문서가 말하지 않는 것들이 여럿이다. 수신자가 실재하는 계정인지 확인하지 않는다. 검증 성공에 대해 토큰이나 세션을 발급하지 않는다. 발송 결과(도달·열람)를 추적하지 않는다. 메일 외의 채널은 다루지 않는다. 링크 클릭 방식 검증은 제공하지 않는다(C-10). 재발송 전용 엔드포인트를 두지 않는다(이건 본문에 있다).
- **근거**: 표준이 지적하는 그대로다. 단순 누락과 의도적 제외는 다른데, 지금 문서에서는 둘을 구분할 수 없다. 앱 팀이 "이건 해 주겠지"라고 가정하면 통합 시점에 드러난다.
- **제안**: 3장 끝에 비목표 목록을 부정형으로 추가한다. 각 항목에 왜 빼는지와 그럼 누가 하는지를 한 줄씩 붙이면 책임 경계(C-04)까지 함께 해결된다.
- **Comment** (게시본)

```text
하드 제약 문단은 좋습니다. 다만 이건 파라미터를 만들지 않는다는 인터페이스 금지이고, 이 서비스가 무엇을 하지 않는가라는 비목표는 따로 필요해 보입니다.

실제로 하지 않는데 문서가 말하지 않는 것들이 있습니다. 수신자가 실재하는 계정인지 확인하지 않고, 검증에 성공해도 토큰이나 세션을 발급하지 않고, 발송 결과나 열람 여부를 추적하지 않고, 메일 외의 채널은 다루지 않습니다. 단순히 안 쓴 것과 의도적으로 뺀 것은 다른데 지금 문서로는 구분이 안 돼서, 앱 팀이 이건 해 주겠지 하고 가정하면 통합할 때 드러납니다.

3장 끝에 하지 않는 것들을 부정형으로 몇 줄 적어 주시고, 각 줄에 왜 빼는지와 그러면 누가 하는지를 붙여 주시면 책임 경계까지 같이 정리됩니다.
```

- **게시** · thread 87980 · 2026-09-01 22:54 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §0.2에 비목표를 표로 정리했습니다 — 수신자 실재 확인 안 함, 검증 성공 시 토큰·세션·서명 미발급, 발송 결과·열람 추적 안 함, 메일 외 채널 안 다룸, 매직링크 방식 미지원. 각 줄에 왜 빼는지와 그럼 누가 하는지를 붙여 책임 경계까지 함께 정리했습니다.
- 조치: §0.2 비목표 표 5행 + 책임 주체 열 · 상태: **Resolved(fixed)**

## C-04 · docs/otp-mail-api-spec.md:4 · 🟡 · 사용자 계층과 책임 경계가 없다 · [thread 87981] · **Resolved(fixed)**

- **원문**
  > `코드 생성 → 메일 발송 → 검증까지 이 서비스가 맡고, 앱은 API 키로 두 번 호출한다.`
- **기준**: spec-standard.md §2.5 User Classes and Characteristics · abc-spec-review 체크리스트 L(구현 착수 가능성)
- **문제**: "앱"이 최종 사용자 단말인지 앱 서버인지 문서 어디에도 없다. 여기에 더해 이 시스템에는 최소 네 부류가 등장하는데(최종 사용자, 각 제품의 서버, 온보딩을 하는 운영자, 인프라 운영자) 각자가 무엇을 하고 무엇을 책임지는지가 정리돼 있지 않다.
- **근거**: 키가 모바일 앱이나 브라우저에 실리면 추출은 시간 문제이고 그 순간 이 설계의 전제가 통째로 무너진다. 소스 IP 허용목록 덕분에 현실적으로는 서버만 통과하지만 그건 결과이지 계약이 아니다. 온보딩 담당자가 넓은 대역을 넣는 순간(2장은 사무실 IP 병기를 이미 허용한다) 그 결과마저 사라진다.
- **제안**: "호출자는 각 제품의 서버여야 하며 키를 최종 사용자 단말에 배포하지 않는다"를 3장 하드 제약과 같은 무게로 적는다. 그 아래에 네 부류의 책임을 표로 정리한다.
- **Comment** (게시본)

```text
흐름 설명에 앱이 API 키로 두 번 호출한다고 되어 있는데, 여기서 앱이 최종 사용자 단말인지 각 제품의 서버인지 문서에서 구분이 안 됩니다.

키가 모바일 앱이나 브라우저에 실리면 추출은 시간 문제이고, 그 순간 이 설계의 전제가 무너집니다. 소스 IP 허용목록 덕분에 현실적으로는 서버만 통과하겠지만 그건 결과이지 계약은 아니라서, 온보딩할 때 넓은 대역이 들어가면 그 결과마저 사라집니다. 문서가 이미 사무실 IP 병기를 허용하고 있어서 더 그렇습니다.

호출자는 각 제품의 서버여야 하며 키를 최종 사용자 단말에 배포하지 않는다는 문장을 하드 제약과 같은 무게로 적어 주시면 좋겠습니다. 겸해서 최종 사용자와 제품 서버와 온보딩 운영자와 인프라 운영자가 각각 무엇을 책임지는지 표로 한 번 정리해 두면 통합할 때 질문이 줄 것 같습니다.
```

- **게시** · thread 87981 · 2026-09-01 22:54 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §0.3에 '호출자는 각 제품의 서버여야 하며 API 키를 최종 사용자 단말에 배포하지 않는다'를 하드 제약과 같은 무게로 명시하고, 소스 IP는 결과이지 계약이 아니라는 점도 적었습니다. 최종 사용자·제품 서버·온보딩 운영자·인프라 운영자의 책임을 표로 정리했습니다.
- 조치: §0.3 호출자=제품 서버 하드 제약 + 4주체 책임 표 · 상태: **Resolved(fixed)**

## C-05 · docs/otp-mail-api-spec.md:14 · 🟡 · 가정과 종속이 한곳에 없다 · [thread 87982] · **Resolved(fixed)**

- **원문**
  > `| 항목 | 값 |`
- **기준**: spec-standard.md §2.6 Assumptions and Dependencies
- **문제**: 이 서비스는 여러 외부 조건 위에 서 있는데 그 목록이 없다. 문서와 테라폼 주석에 흩어져 있는 것만 모아도 이렇다. 계정이 이미 ewoosoft.com 도메인 식별자와 프로덕션 발송 권한을 가지고 있다는 것, 그 도메인의 DKIM 레코드를 우리가 아니라 사내 IT·보안팀이 소유한다는 것, 각 앱이 고정 송신 주소를 확보할 수 있다는 것, cleverspacecloud.com 아피스 존이 같은 계정의 라이브 존이라는 것, 시크릿 캐시가 AWS가 게시하는 확장 레이어에 의존한다는 것.
- **근거**: 이 중 하나만 깨져도 서비스가 서지 않는데, 흩어져 있으면 무엇이 깨졌을 때 무엇이 멈추는지 추적할 수 없다. 특히 DKIM 소유가 외부 조직이라는 점은 장애 시 우리가 손댈 수 없는 영역이라는 뜻이라 문서에 있어야 한다.
- **제안**: 상단 표 아래에 가정과 종속 절을 만들고, 각 항목에 "깨지면 무엇이 멈추는가"를 한 줄씩 붙인다.
- **Comment** (게시본)

```text
이 서비스가 서 있는 외부 조건들이 문서와 테라폼 주석에 흩어져 있어서 한곳에 모아 주시면 좋겠습니다.

모아 보면 계정이 이미 도메인 식별자와 프로덕션 발송 권한을 가지고 있다는 것, 그 도메인의 DKIM 레코드를 우리가 아니라 사내 IT 보안팀이 소유한다는 것, 각 앱이 고정 송신 주소를 확보할 수 있다는 것, 아피스 존이 같은 계정의 라이브 존이라는 것, 시크릿 캐시가 AWS가 게시하는 확장 레이어에 의존한다는 것 정도가 됩니다.

이 중 하나만 깨져도 서비스가 서지 않는데 흩어져 있으면 무엇이 깨졌을 때 무엇이 멈추는지 추적이 어렵습니다. 특히 DKIM 소유가 외부 조직이라는 점은 장애가 났을 때 우리가 손댈 수 없는 영역이라는 뜻이라 문서에 명시적으로 있는 편이 낫습니다. 상단 표 아래에 절을 하나 만들고 각 항목에 깨지면 무엇이 멈추는지 한 줄씩 붙여 주시면 충분할 것 같습니다.
```

- **게시** · thread 87982 · 2026-09-01 22:55 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §0.5에 외부 종속성을 한 곳에 모으고 각 항목에 '깨지면 무엇이 멈추는지'를 붙였습니다. 특히 DKIM 레코드가 사내 IT 보안팀 소유(우리 관리 밖)라 장애 시 우리가 손댈 수 없는 영역임을 명시했습니다.
- 조치: §0.5 종속성 표 + '깨지면 멈추는 것' · 상태: **Resolved(fixed)**

## C-06 · docs/otp-mail-api-spec.md:198 · 🔴 · 성능과 가용성 요구가 없다 · [thread 87983] · **Resolved(fixed)**

- **원문**
  > `- Lambda 타임아웃 15초, API GW 스테이지 스로틀 5 rps / burst 10.`
- **기준**: spec-standard.md 5장 Performance Requirements · §6.3.1 Availability · abc-spec-review 체크리스트 G·L
- **문제**: 여기 적힌 것은 구현 설정값이지 요구사항이 아니다. 발급 요청을 받고 몇 초 안에 메일이 도착해야 하는지, 검증 응답이 몇 밀리초 안에 와야 하는지, 이 서비스가 어느 수준으로 살아 있어야 하는지가 없다. 동시 세션 목표도 없다.
- **근거**: 숫자가 없으면 합격 불합격 판정이 불가능하다. 특히 이 서비스는 세 제품의 로그인 경로에 직렬로 놓이므로 가용성 목표가 곧 그 제품들의 로그인 가용성 상한이 된다. 그 값을 앱 팀이 알아야 자기 쪽 타임아웃과 실패 처리를 설계할 수 있다. 또 스로틀 5 rps는 스테이지 전체에 걸리는 값이라 테넌트별 격리가 없는데, 한 제품의 호출이 다른 제품의 로그인을 막을 수 있다는 사실이 문서에 없다.
- **제안**: 발송 지연 목표(예: 95퍼센트가 30초 이내 도달), 검증 응답 목표(예: 95퍼센트가 500밀리초 이내), 가용성 목표, 동시 처리 목표를 수치로 적는다. 스로틀이 전체 공유임을 함께 명시한다.
- **Comment** (게시본)

```text
내부 설계 절에 람다 타임아웃과 스로틀 값이 있는데, 이건 구현 설정값이고 요구사항으로서의 목표치가 문서에 없습니다.

발급 요청을 받고 몇 초 안에 메일이 도착해야 하는지, 검증 응답이 몇 밀리초 안에 와야 하는지, 이 서비스가 어느 수준으로 살아 있어야 하는지가 정해져 있지 않으면 합격 불합격 판정을 할 수 없습니다. 특히 이 서비스는 세 제품의 로그인 경로에 직렬로 놓여서 여기 가용성 목표가 곧 그 제품들의 로그인 가용성 상한이 됩니다. 앱 팀이 그 값을 알아야 자기 쪽 타임아웃과 실패 처리를 설계할 수 있습니다.

같은 자리에서 스로틀 5 rps가 스테이지 전체에 걸리는 값이라는 점도 적어 주시면 좋겠습니다. 테넌트별 격리가 아니라서 한 제품의 호출이 다른 제품의 로그인을 함께 막을 수 있는데 지금 문서로는 알기 어렵습니다.
```

- **게시** · thread 87983 · 2026-09-01 22:56 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §0.6에 성능·가용성 목표 절을 만들고, 이 서비스가 세 제품 로그인 경로에 직렬로 놓여 여기 가용성 목표가 곧 그 제품들의 로그인 가용성 상한이 된다는 점을 적었습니다. 다만 목표치는 아직 제안값이라 미결 #5로 두었습니다. 스로틀 5 rps가 스테이지 전역 값이라 테넌트별 격리가 아니라는 점(한 제품 호출이 다른 제품 로그인을 막을 수 있음)도 §0.6·§5·결정기록 D-06에 명시했습니다.
- 조치: §0.6에 **현재 구성 보장 최악값 열 신설**(발급·검증 = 콜드스타트 포함 Lambda 15초 상한, 가용성 = 단일 리전·함수·테이블 상한). 확정 목표는 미결 #5 유지 · 상태: **Resolved(fixed)**

## C-07 · docs/otp-mail-api-spec.md:179 · 🔴 · 예상 데이터 규모가 없다 · [thread 87984] · **Resolved(fixed)**

- **원문**
  > `발송 상한이 존재하는 이유도 이것이다: 이 계정은 월 ~21건만 보내서 바운스 2건이면`
- **기준**: abc-spec-review 체크리스트 E-1(데이터 규모 — 하드) · spec-standard.md §6.4
- **문제**: 여기 나오는 월 21건은 이 서비스가 붙기 *전* 계정의 과거 실적이다. 세 제품이 다섯 리전에서 붙은 뒤의 예상 발송량은 문서 어디에도 없다.
- **근거**: 이 숫자가 거의 모든 후속 결정의 입력이다. 시간당 상한을 10으로 정한 근거, 스로틀 5 rps가 충분한지, SES 발송 한도와 비용, 바운스율 경보의 임계, DynamoDB 용량 모드 선택이 전부 여기서 나온다. 규모가 스펙에 없으면 아무도 검증하지 않는다. 게다가 4장의 바운스 위험 논증 자체가 "월 21건이라 분모가 작다"에 기대고 있는데, 실제 운영 발송량이 월 수만 건이 되면 그 논증의 전제가 바뀐다.
- **제안**: 제품별·리전별 월 예상 발급 건수와 증가율, 피크 시간대 동시 요청 추정치를 적는다. 정확한 수치를 지금 알 수 없으면 자릿수 단위 추정과 그 근거(예상 활성 사용자 × 로그인 빈도)라도 남기고, 실측 후 갱신 시점을 미결에 넣는다.
- **Comment** (게시본)

```text
운영 규칙 절의 월 21건은 이 서비스가 붙기 전 계정의 과거 실적으로 읽힙니다. 세 제품이 다섯 리전에서 붙은 뒤의 예상 발송량은 문서에 없는 것 같습니다.

이 숫자가 뒤따르는 결정 대부분의 입력이라 비어 있으면 근거가 서지 않습니다. 시간당 상한을 10으로 정한 근거, 스로틀 값이 충분한지, SES 한도와 비용, 바운스율 경보의 임계가 전부 여기서 나옵니다. 특히 바운스 위험 논증 자체가 분모가 작다는 데 기대고 있어서, 실제 운영 발송량이 월 수만 건이 되면 그 논증의 전제가 바뀝니다.

제품별 리전별 월 예상 발급 건수와 증가율, 피크 시간대 동시 요청 추정치를 적어 주시면 좋겠습니다. 지금 정확히 알기 어려우면 자릿수 단위 추정과 그 근거만 남기고 실측 후 갱신하는 것으로 미결에 넣어 두어도 될 것 같습니다.
```

- **게시** · thread 87984 · 2026-09-01 22:57 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §0.7에 '월 21건은 붙기 전 과거 실적'임을 명시하고, 세 제품 다섯 리전 붙은 뒤 예상 발송량은 미측정으로 미결 #6에 두었습니다. 바운스 위험 논증이 '분모가 작다'에 기대고 있어 실제 발송량이 커지면 전제가 바뀐다는 점도 적었습니다. 자릿수 추정과 근거는 실측 후 갱신하겠습니다.
- 조치: §0.7 무변경 — 자릿수 추정 없이 미결 #6 유지. 재확인 시점만 '첫 온보딩 직후'로 기입 · **2026-09-03 리뷰 종료로 close**(잔여는 C-40·C-47, 미게시) · 상태: **Resolved(fixed)**

## C-08 · docs/otp-mail-api-spec.md:154 · 🟡 · 결정 근거가 추적 가능한 형태로 남아 있지 않다 · [thread 87985] · **Resolved(fixed)**

- **원문**
  > `생성·검증까지 이 서비스가 맡는 것(B안)은 2026-09-01 앱 팀 협의 결과다 — 여러`
- **기준**: spec-standard.md "논의 기록(Decision Log)" · "핵심 결정 — 대안 검토 박스 의무화" · abc-spec-review 체크리스트 J
- **문제**: B안이라는 표현이 문서 여러 곳에 나오는데 A안이 무엇이었는지가 없다. 되돌리기 비싼 결정이 여럿인데(인증을 키와 소스 IP로 한 것, 단일 테이블 두 행 종류, 고정 시간 버킷, 6자리와 600초, 발송 상한 통과 허용) 각각에 대해 무엇과 비교했고 왜 채택했으며 언제 재검토하는지가 정리돼 있지 않다. 본문에 근거 문장이 흩어져 있기는 하지만 추적 가능한 형태는 아니다.
- **근거**: 표준이 대안 검토 박스를 의무화하는 대상(인증·세션 방식, 저장 전략, 비기능 수치)에 이 결정들이 정확히 해당한다. 6개월 뒤 같은 논의를 반복하지 않으려면 결론 옆에 비교안과 재검토 조건이 있어야 한다.
- **제안**: 문서 끝에 결정 기록 표를 붙인다. 일시, 결정 사항, 채택안, 비교 대안, 채택 이유, 결정자, 관련 절 정도면 충분하다. 인증 방식과 발송 상한 정책 두 개는 대안 검토를 조금 더 풀어 쓰는 편이 좋다.
- **Comment** (게시본)

```text
문서 여러 곳에 B안이라는 표현이 나오는데 A안이 무엇이었는지는 문서에 없습니다. 협의 결과라는 사실만 남고 비교 대상이 사라진 상태입니다.

되돌리기 비싼 결정이 여럿 있습니다. 인증을 키와 소스 IP로 한 것, 단일 테이블에 두 종류의 행을 넣은 것, 고정 시간 버킷을 쓴 것, 자릿수와 유효시간을 지금 값으로 정한 것, 발송 상한을 통과 허용으로 둔 것입니다. 본문에 근거 문장이 흩어져 있기는 한데 나중에 찾아 읽을 수 있는 형태는 아닙니다.

문서 끝에 결정 기록을 표로 붙여 주시면 좋겠습니다. 일시와 결정 사항과 채택안과 비교 대안과 채택 이유와 관련 절 정도면 충분합니다. 그중 인증 방식과 발송 상한 정책 두 개는 어떤 대안을 왜 기각했고 어떤 조건에서 다시 볼지를 조금 더 풀어 써 두면 나중에 같은 논의를 반복하지 않게 됩니다.
```

- **게시** · thread 87985 · 2026-09-01 22:57 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §7에 결정 기록 표를 붙였습니다 — ID·일시·결정·채택안·기각/대안·이유·다시 볼 조건. A안(앱이 생성·검증, 인프라는 발송만)도 D-01에 남겼습니다. 인증 방식과 발송 상한 정책(D-02)은 어떤 대안을 왜 기각했고 어떤 조건에서 다시 볼지를 조금 더 풀어 적었습니다.
- 조치: §7 결정 기록 표 10행(D-01~D-10) · 상태: **Resolved(fixed)**

## C-09 · docs/otp-mail-api-spec.md:260 · 🟡 · 미결 항목에 담당자와 기한과 티켓이 없다 · [thread 87986] · **Resolved(fixed)**

- **원문**
  > `## 6. 미결`
- **기준**: spec-standard.md "TBD 처리 규칙" · abc-spec-review 체크리스트 H
- **문제**: 네 항목 모두 상태만 있고 누가 언제까지 무엇을 근거로 닫는지가 없다. 특히 3번(앱별 송신 주소)은 이 서비스를 실제로 쓸 수 있느냐를 좌우하는데 "수집 중"으로만 되어 있다.
- **근거**: 담당자와 기한이 없는 미결은 baseline 시점에 그대로 남는다. 표준이 TBD에 담당자·결정 시점·관련 이슈를 요구하는 이유가 그것이다.
- **제안**: 표에 담당자, 목표 시점, 관련 티켓 열을 추가한다. 4번(코드 소유 레포)은 코드 배포권 이관 시점과 묶여 있으므로 그 순서도 함께 적는다.
- **Comment** (게시본)

```text
미결 표에 상태는 있는데 누가 언제까지 무엇을 근거로 닫는지가 없습니다. 담당자와 기한이 없는 미결은 대체로 그대로 남습니다.

특히 앱별 송신 주소 항목은 이 서비스를 실제로 쓸 수 있느냐를 좌우하는데 수집 중으로만 되어 있어서, 담당자와 목표 시점이 있으면 좋겠습니다. 코드 소유 레포 항목도 배포권 이관 시점과 묶여 있으니 그 순서를 같이 적어 두면 나중에 헷갈리지 않을 것 같습니다.

표에 담당자와 목표 시점과 관련 티켓 열을 추가하는 정도면 충분해 보입니다.
```

- **게시** · thread 87986 · 2026-09-01 22:58 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §6 미결 표에 담당·목표시점·관련 티켓 열을 추가했습니다. 앱별 송신 주소는 각 애플리케이션들의 VM public ip 및 NAT Gateway IP를 허용하도록 지정하였고 코드 소유 레포·배포권 이관에 티켓을 연결했습니다. 목표시점은 기획·팀 확정 대기라 아직 미정으로 두었습니다.
- 조치: §6 미결 표에 담당·목표시점·티켓 열 추가(목표시점은 전건 미정) · 상태: **Resolved(fixed)**

---

# B. 제품 결정 — 검증 흐름

## C-10 · docs/otp-mail-api-spec.md:7 · 🔴 · 메일 링크 클릭 방식이 검토되지 않았고, 현 구조로는 나중에 얹기 어렵다 · [thread 87987] · **Resolved(fixed)**

- **원문** (7~11줄, 흐름 블록)
  > `1. 사용자가 앱에 로그인 시도` / `2. 앱 → POST /v1/otp {email}` / `3. 사용자가 메일의 6자리 코드를 앱에 입력` / `4. 앱 → POST /v1/otp/verify {email, code}` / `5. {"verified": true} 받으면 앱이 자기 세션 승격`
- **기준**: spec-standard.md §1.2 Product Scope(핵심 역할과 경계) · §2.3 Overall Operation · abc-spec-review 체크리스트 M(장기 지속성 — 되돌리기 비싼 결정)
- **문제**: 이메일 OTP의 검증 방식은 통상 두 가지다. 하나는 지금 문서가 다루는 코드 입력 방식이고, 다른 하나는 메일 안의 링크를 눌러 확인 화면을 보고 원래 화면이 다음 단계로 넘어가는 방식이다. 두 번째가 검토 대상에 없고, 비목표로도 적혀 있지 않다.
- **근거**: 더 중요한 것은 현재 구조가 그 방식을 *수용할 수 없다*는 점이다. 세 가지가 동시에 막는다.
  1. **인증 모델**: 모든 호출이 `x-api-key` + 소스 IP 허용목록을 요구한다(27줄). 링크를 누르는 주체는 최종 사용자의 브라우저이므로 이 API에 도달할 수 없다. 즉 링크 방식은 무인증으로 열리는 별도 경로를 필요로 하고, 이는 지금 인증 모델의 예외가 된다.
  2. **저장 구조**: 코드 행의 키가 `code#{tenant}#{email}`뿐이라(222줄) 특정 발급 시도를 가리키는 식별자가 없다. 링크 방식은 링크에 담을 일회용 식별자와, 대기 중인 화면이 결과를 조회할 수단(폴링 또는 푸시)을 필요로 한다. 지금은 둘 다 없다.
  3. **템플릿과 하드 제약**: 템플릿에 링크 자리가 없고, 링크는 제품별 복귀 URL을 요구하는데 3장 하드 제약이 호출자가 문구·URL을 정하는 것을 금지한다. 즉 링크 방식은 하드 제약과 직접 충돌하며, 그 충돌이 문서에 인지돼 있지 않다.
  지금 결정하면 저장 키에 식별자 한 자리를 더하고 조회 경로 하나를 여는 수준이다. 나중에 요구가 오면 저장 구조와 인증 모델과 템플릿을 동시에 바꿔야 한다. 되돌리기 비싼 결정의 전형이다.
- **제안**: 둘 중 하나를 문서에 남긴다. (가) 링크 방식을 지원하지 않기로 한다면 비목표에 넣고, 왜 코드 입력 방식만 택했는지와 그 결정이 인증 모델과 저장 키를 어떻게 고정하는지를 적는다. (나) 언젠가 지원할 가능성이 있다면 지금 저장 키에 발급 시도 식별자를 넣어 두고, 링크가 도달할 경로의 인증을 어떻게 다르게 할지(짧은 수명의 서명된 토큰 등)를 한 문단으로 남긴다.
- **Comment** (게시본)

```text
문서 상단 흐름은 사용자가 메일의 숫자를 화면에 입력하는 방식만 다루고 있습니다. 이메일 OTP에는 보통 다른 방식이 하나 더 있습니다. 메일 안의 링크를 누르면 확인되었습니다 화면이 뜨고 원래 열려 있던 화면이 다음 단계로 넘어가는 방식입니다. 이 방식이 검토 대상에 없고 하지 않기로 한 것이라는 언급도 없어서 여쭙니다.

더 신경 쓰이는 부분은 지금 구조가 그 방식을 나중에 얹기 어렵게 되어 있다는 점입니다. 세 가지가 동시에 걸립니다. 인증이 키와 소스 IP라서 링크를 누르는 주체인 사용자 브라우저는 이 API에 도달할 수 없습니다. 저장된 코드의 키가 테넌트와 수신자 조합뿐이라 특정 발급 시도를 가리키는 식별자가 없고, 그래서 대기 중인 화면이 결과를 알아낼 방법이 없습니다. 그리고 링크에는 제품별 복귀 주소가 필요한데 호출자가 문구나 주소를 정하는 것은 하드 제약이 막고 있습니다.

지금 단계에서는 저장 키에 식별자 한 자리를 더하고 조회 경로 하나를 여는 정도로 끝나지만, 나중에 요구가 오면 저장 구조와 인증 모델과 템플릿을 한꺼번에 바꿔야 합니다.

지원하지 않기로 하셨다면 그 판단을 비목표에 적고 그 결정이 인증 모델과 저장 키를 어떻게 고정하는지 한 줄 붙여 주시면 충분합니다. 가능성을 열어 두신다면 지금 저장 키에 발급 시도 식별자를 넣어 두고, 링크가 도달할 경로의 인증을 어떻게 다르게 할지를 한 문단 정도로 남겨 두시는 편이 나중에 훨씬 쌉니다.
```

- **게시** · thread 87987 · 2026-09-01 22:58 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 매직링크 방식은 §0.2 비목표에 넣고, 왜 지금 구조가 이를 못 얹는지는 결정기록 D-07에 정리했습니다 — 인증이 키+소스 IP라 사용자 브라우저가 도달 못 하고, 저장 키가 테넌트+수신자뿐이라 특정 발급을 가리키는 식별자가 없어 대기 화면이 결과를 못 얻으며, 링크엔 제품별 복귀 주소가 필요한데 하드 제약이 막습니다. 저장 키에 용도/식별자 축을 더하는 대응(D-07)과 묶어 두었습니다.
- **[임건혁(Jack) · 2026-09-03 02:10 · cid 3]**
  > 개발팀장 확인 결과 코드 입력 방식으로 확정됐습니다 — 메일 링크 클릭 자동 인증 URL은 고려하지 않습니다(2026-09-02). §0.2 비목표 표의 매직링크 행을 '코드 입력 방식으로 확정, 자동 인증 URL 미고려'로 명시하고, 결정기록 D-07의 남는 사유를 매직링크가 아니라 purpose 축(두 번째 용도)으로 좁혔습니다. 지적하신 대로 이 전제가 답변에만 남지 않도록 문서에 근거로 박았습니다.
- 조치: §0.2 비목표 등재 + D-07 연결. **2026-09-03 개발팀장 확정** — 코드 입력 방식, 자동 인증 URL 미고려. D-07의 남는 사유를 purpose 축으로 축소 · 상태: **Resolved(fixed)**

## C-11 · docs/otp-mail-api-spec.md:11 · 🟡 · 검증 성공이 무엇을 보증하는지와 바인딩 책임이 정의돼 있지 않다 · [thread 87988] · **Resolved(fixed)**

- **원문**
  > `5. {"verified": true} 받으면 앱이 자기 세션 승격`
- **기준**: spec-standard.md §6.2 Security Requirements · §6.5 Business Rules · abc-spec-review 체크리스트 L
- **문제**: 이 서비스는 참 또는 거짓만 돌려주고 그 결과에 결부된 토큰이나 서명을 주지 않는다. 그러면 앱 서버가 "지금 이 세션이 그 이메일 주인이 맞다"를 어떻게 묶는지가 앱 책임이 되는데, 그 경계가 문서에 없다. 응답에 어떤 요청에 대한 답인지 식별할 값도 없어서, 앱 쪽에서 서로 다른 사용자 요청의 응답이 뒤섞이지 않도록 하는 책임도 전적으로 앱에 있다.
- **근거**: OTP는 인증 경로이므로 "무엇이 보증되고 무엇이 보증되지 않는가"가 계약의 일부다. 지금 문서는 5번 줄 하나로 넘어간다.
- **제안**: 검증 성공이 보증하는 것(해당 시점에 그 주소로 발송된 코드를 제시했다는 사실)과 보증하지 않는 것(세션 신원 결속, 재생 방지, 사용자 존재)을 명시하고, 앱이 무엇을 해야 하는지 한 문단으로 적는다.
- **Comment** (게시본)

```text
흐름 마지막 줄이 검증 결과를 받으면 앱이 자기 세션을 승격한다로 되어 있는데, 이 서비스는 참 거짓만 돌려주고 그 결과에 붙는 토큰이나 서명은 주지 않습니다. 그러면 지금 이 세션이 그 이메일의 주인이 맞다는 것을 앱 서버가 어떻게 묶는지가 앱 책임이 되는데 그 경계가 문서에 없습니다.

응답에 어떤 요청에 대한 답인지 식별할 값도 없어서, 서로 다른 사용자 요청의 응답이 뒤섞이지 않게 하는 책임도 전적으로 앱 쪽에 있습니다.

검증 성공이 보증하는 것과 보증하지 않는 것을 한 문단으로 적어 주시면 좋겠습니다. 보증하는 것은 그 시점에 그 주소로 발송된 코드를 제시했다는 사실 정도이고, 세션 신원 결속이나 재생 방지나 사용자 존재는 보증하지 않는다는 식입니다. 그리고 그래서 앱이 무엇을 해야 하는지를 한 줄 붙여 주시면 앱 팀이 자기 쪽 설계를 시작할 수 있습니다.
```

- **게시** · thread 87988 · 2026-09-01 22:59 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §0.4에 검증 성공이 보증하는 것(그 시점 그 주소로 발송된 코드를 제시했다는 사실)과 보증하지 않는 것(세션 신원 결속, 재생 방지·요청 상관, 사용자 존재)을 나눠 적고, 응답에 요청 식별값이 없어 요청↔응답 상관이 앱 책임이라는 점도 명시했습니다.
- 조치: §0.4 보증/비보증 분리 서술 · 상태: **Resolved(fixed)**

---

# C. 확장성 (5~10년)

## C-12 · docs/otp-mail-api-spec.md:45 · 🔴 · 언어가 둘뿐인데 문서 자신이 다국 테넌트를 예로 든다 · [thread 87989] · **Resolved(fixed)**

- **원문**
  > `| \`locale\` | - | \`ko\` \| \`en\`. **기본 en**, \`"ko"\`일 때만 한국어 |`
  > (135줄) `- **테넌트 명명: \`<제품>-<리전>\`** (dent-vn, dent-eg, lab-kr, rc-bra, rc-au).`
- **기준**: spec-standard.md §6.10 Internationalization · abc-spec-review 체크리스트 D(섹션 간 일관성)·M
- **문제**: 1장은 언어를 한국어와 영어 둘로 못 박고, 2장은 테넌트 예시로 베트남·이집트·한국·브라질·호주를 든다. 같은 문서 안에서 어긋난다. 베트남어·아랍어·포르투갈어 사용자가 영어 인증 메일을 받는 것이 의도된 결정인지, 아니면 아직 안 정한 것인지 문서로는 알 수 없다.
- **근거**: 템플릿이 함수 코드 안에 상수로 박혀 있어 언어를 하나 추가하려면 코드 배포가 필요하다. 그런데 코드 배포권은 앱 레포 CI로 넘어갈 예정이라(미결 4번) 언어 추가의 소유자가 누구인지도 정해져 있지 않다. 오른쪽에서 왼쪽으로 쓰는 언어(아랍어)가 들어오면 본문 구성도 손봐야 한다.
- **제안**: 어느 테넌트가 어느 언어를 쓰는지 표로 적고, 언어를 추가하는 절차와 소유자를 정한다. 지금 영어로 통일하기로 한 것이라면 그 판단을 명시하고 재검토 조건(현지 제품 정식 출시 시 등)을 붙인다.
- **Comment** (게시본)

```text
언어 항목이 한국어와 영어 둘로 되어 있는데, 온보딩 절의 테넌트 예시는 베트남과 이집트와 한국과 브라질과 호주입니다. 같은 문서 안에서 어긋나 보입니다.

베트남어나 아랍어나 포르투갈어를 쓰는 사용자가 영어 인증 메일을 받는 것이 의도한 결정인지, 아직 안 정한 것인지 문서로는 구분이 안 됩니다. 인증 메일은 사용자가 받는 몇 안 되는 메일이라 현지 제품팀에서 요구가 올 가능성이 높은 항목이기도 합니다.

템플릿이 함수 코드 안에 상수로 들어 있어서 언어를 하나 늘리려면 코드 배포가 필요한데, 코드 배포권은 앱 레포로 넘어갈 예정이라 언어 추가의 소유자도 아직 정해지지 않은 상태로 보입니다. 오른쪽에서 왼쪽으로 쓰는 언어가 들어오면 본문 구성도 손봐야 합니다.

어느 테넌트가 어느 언어를 쓰는지 표로 적고 언어를 추가하는 절차와 소유자를 정해 주시면 좋겠습니다. 지금은 영어로 통일하기로 한 것이라면 그 판단과 다시 볼 조건을 적어 두는 것만으로 충분합니다.
```

- **게시** · thread 87989 · 2026-09-01 22:59 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §1 템플릿 절에 '비영어권 사용자가 영어 메일을 받는 것은 현재 en 통일 결정이지 확정 다국어 지원이 아님'을 명시하고(결정기록 D-03), 언어 추가는 코드 배포가 필요하며 소유자가 앱 레포 이관과 묶여 미정임을 적었습니다. RTL 언어 시 본문 구성도 손봐야 함을 남겼습니다. 어느 테넌트가 어느 언어인지의 확정과 추가 절차·소유자는 미결 #2로 두었습니다.
- 조치: §1에 en 통일 결정 명시 + D-03 + 미결 #2 · 상태: **Resolved(fixed)**

## C-13 · docs/otp-mail-api-spec.md:104 · 🟡 · 제품 브랜딩이 들어갈 자리가 없다 · [thread 87990] · **Resolved(fixed)**

- **원문**
  > `| locale | 제목 | 본문 |`
  > (106줄) `| \`ko\` | [VATECH] 인증 코드 | 인증 코드 + 10분 유효 안내 + 미요청 시 무시 안내 |`
- **기준**: abc-spec-review 체크리스트 M(하드코딩된 정책 값)
- **문제**: 세 제품 사용자가 모두 같은 제목의 메일을 받는다. Clever Dent를 쓰는 사용자에게 다른 브랜드 이름이 붙은 인증 메일이 가는 셈이다.
- **근거**: 제품명을 넣어 달라는 요구는 거의 확실히 온다. 그런데 3장 하드 제약이 호출자가 제목·본문을 정하는 것을 금지하므로, 요구가 왔을 때 취할 수 있는 길은 하나뿐이다. 인증된 테넌트를 보고 **서버가** 템플릿을 고르는 것이다. 이 방식은 하드 제약을 전혀 훼손하지 않는다(문구는 여전히 서버 소유). 다만 지금 설계에 그 이음매가 없어서, 요구가 오면 하드 제약을 깨자는 논의로 흐를 위험이 있다.
- **제안**: 템플릿을 테넌트 축으로 선택하는 구조를 지금 열어 둔다. 클라이언트 맵의 테넌트 항목에 템플릿 식별자를 두고, 문구 자체는 서버가 소유한다는 원칙을 명시한다. 당장 값이 하나뿐이어도 자리를 만들어 두는 비용은 거의 없다.
- **Comment** (게시본)

```text
메일 템플릿이 제목까지 고정이라 세 제품 사용자가 모두 같은 제목의 메일을 받게 됩니다. 다른 제품을 쓰는 사용자에게 그 제품이 아닌 브랜드 이름이 붙은 인증 메일이 가는 셈입니다.

제품명을 넣어 달라는 요구는 언젠가 올 가능성이 높다고 보는데, 그때 취할 수 있는 길은 사실상 하나입니다. 인증된 테넌트를 보고 서버가 템플릿을 고르는 방식입니다. 문구는 여전히 서버가 소유하니 하드 제약을 훼손하지 않습니다. 다만 지금 설계에 그 이음매가 없어서, 요구가 왔을 때 파라미터를 열자는 쪽으로 논의가 흐를 여지가 있어 보입니다.

클라이언트 맵의 테넌트 항목에 템플릿 식별자를 하나 두고 문구 자체는 서버가 소유한다는 원칙을 문서에 적어 두시면 어떨까 합니다. 지금은 값이 하나뿐이어도 자리를 만들어 두는 비용은 거의 없습니다.
```

- **게시** · thread 87990 · 2026-09-01 23:00 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 제품별 템플릿 선택은 '인증된 테넌트를 보고 서버가 템플릿을 고르는 방식'이 유일한 길이라는 데 동의합니다(문구는 서버 소유 유지, 하드 제약 불훼손). 다만 클라이언트 맵에 템플릿 식별자 자리를 여는 것은 코드·시크릿 스키마 변경이라, 지금은 §1에 그 이음매가 없음을 명시하고 결정기록 D-04에 '저비용이니 요구 오면 자리 열기'로 등재했습니다. 코드 반영은 관련 결정들과 함께 처리하겠습니다.
- 조치: §1에 이음매 부재 명시 + D-04 등재(구현 이월) · 상태: **Resolved(fixed)**

## C-14 · docs/otp-mail-api-spec.md:222 · 🟡 · 코드 슬롯 키가 용도를 구분하지 않는다 · [thread 87991] · **Resolved(fixed)**

- **원문**
  > `pk = "code#{tenant}#{email 소문자}"`
- **기준**: abc-spec-review 체크리스트 M(데이터 모델이 성장을 견디는가)
- **문제**: 수신자당 활성 코드가 하나라는 규칙이 용도를 가리지 않는다. 같은 사용자가 로그인 인증과 이메일 변경 확인을 비슷한 시점에 진행하면 뒤에 발급된 쪽이 앞의 코드를 지운다. 지금은 로그인 용도 하나뿐이라 드러나지 않지만, 인증 메일을 쓰는 두 번째 용도가 생기는 순간 서로를 무효화한다.
- **근거**: 2장이 테넌트 명명을 설명하면서 이미 같은 논리를 편다. "리전을 하나로 묶으면 같은 사용자가 A리전에서 코드를 받는 순간 B리전 코드가 폐기된다"는 문장이다. 용도 축에도 정확히 같은 논리가 적용되는데 그 축은 키에 없다.
- **제안**: 키를 `code#{tenant}#{용도}#{email}` 형태로 하고 지금은 용도에 상수 하나만 쓴다. 나중에 넣으려면 기존 행 이관이 필요하다.
- **Comment** (게시본)

```text
활성 코드 슬롯이 수신자당 하나인데 이 규칙이 용도를 가리지 않습니다. 같은 사용자가 로그인 인증과 이메일 변경 확인을 비슷한 시점에 진행하면 뒤에 발급된 쪽이 앞의 코드를 지웁니다. 지금은 로그인 용도 하나뿐이라 드러나지 않지만 두 번째 용도가 생기는 순간 서로를 무효화합니다.

테넌트 명명을 설명하신 문단에서 이미 같은 논리를 쓰고 계십니다. 리전을 하나로 묶으면 같은 사용자가 한쪽에서 코드를 받는 순간 다른 쪽 코드가 폐기된다는 대목입니다. 용도 축에도 그대로 적용되는 이야기인데 그 축만 키에 없습니다.

키를 테넌트와 용도와 수신자 조합으로 두고 지금은 용도 자리에 상수 하나만 쓰시면 어떨까 합니다. 나중에 넣으려면 기존 행 이관이 필요해집니다.
```

- **게시** · thread 87991 · 2026-09-01 23:00 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 지적하신 대로 슬롯이 수신자당 하나이고 용도를 가리지 않아, 두 번째 용도가 생기면 서로 무효화합니다. §5에 이 한계를 명시하고, 키를 code#{tenant}#{purpose}#{email}로 두고 지금은 purpose에 상수 하나만 쓰는 대응을 결정기록 D-07에 등재했습니다. 나중에 넣으면 기존 행 이관이 필요하다는 점도 함께 남겼습니다. 이미 apply된 테이블 키 형태 변경이라 별도 결정으로 반영하겠습니다.
- 조치: §5 ⚠ + D-07 등재(구현 이월) · 상태: **Resolved(fixed)**

## C-15 · docs/otp-mail-api-spec.md:252 · 🟡 · 채널이 메일로 고정돼 있고 확장 경로가 없다 · [thread 87992] · **Resolved(fixed)**

- **원문**
  > `| SESv2 SDK | SMTP (또는 Pod Identity로 SES 유지) | 어댑터 + \`wiring.mjs\` |`
- **기준**: abc-spec-review 체크리스트 M · spec-standard.md 비목표 규약
- **문제**: 이관 계약 표는 같은 채널(메일) 안에서 발송 수단이 바뀌는 경우만 다룬다. 문자나 앱 푸시로 OTP를 보내 달라는 요구가 오면 무엇이 바뀌는지 문서에 없다. 이름부터 테이블 키 접두사까지 메일 전제가 깔려 있다.
- **근거**: 다중 제품 공용 인증 모듈에서 채널 확장 요구는 드물지 않다. 지금 구조에서 채널을 늘리려면 발송 포트뿐 아니라 수신자 식별자 형태(이메일 주소 가정)와 검증 흐름까지 손대야 한다.
- **제안**: 다루지 않기로 한 것이면 비목표에 넣는다. 여지를 둘 것이면 수신자 식별자를 채널 중립적으로 다루는 방향만 한 문단 적어 둔다.
- **Comment** (게시본)

```text
이관 계약 표는 같은 채널 안에서 발송 수단이 바뀌는 경우를 다루고 있는데, 문자나 앱 푸시로 OTP를 보내 달라는 요구가 왔을 때 무엇이 바뀌는지는 문서에 없습니다.

지금은 모듈 이름부터 저장 키 접두사까지 메일 전제가 깔려 있어서, 채널을 늘리려면 발송 부분만이 아니라 수신자 식별자 형태와 검증 흐름까지 손대야 합니다.

다루지 않기로 한 것이면 하지 않는 것 목록에 넣어 주시면 되고, 여지를 두실 생각이면 수신자 식별자를 채널에 중립적으로 두는 방향만 한 문단 적어 두셔도 좋겠습니다.
```

- **게시** · thread 87992 · 2026-09-01 23:01 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > SMS·푸시 채널은 §0.2 비목표에 넣고, 채널을 늘리려면 발송부만이 아니라 수신자 식별자 형태·검증 흐름까지 손대야 한다는 점을 §5와 결정기록 D-08에 적었습니다. 지금은 다루지 않기로 하되, 요구가 오면 수신자 식별자를 채널 중립으로 두는 방향을 D-08에서 다시 보기로 했습니다.
- 조치: §0.2 비목표 + §5 + D-08 · 상태: **Resolved(fixed)**

## C-16 · docs/otp-mail-api-spec.md:80 · 🟡 · 정책 값을 테넌트별로 다르게 갈 수 없다 · [thread 87993] · **Resolved(fixed)**

- **원문**
  > `| 항목 | 값 |` (코드 정책 표 — 자릿수 6, 유효시간 600초, 시도 5회, 동시 활성 1개)
- **기준**: abc-spec-review 체크리스트 M(하드코딩된 정책 값이 코드 변경 없이 바뀌는가)
- **문제**: 네 값이 모두 전역 상수다. 제품마다 다른 값을 요구하는 것은 흔한 일인데(의료 제품 쪽에서 유효시간을 더 짧게 요구하거나, 반대로 메일 지연이 큰 지역에서 더 길게 요구), 지금 구조로는 전 테넌트가 같은 값을 쓴다. 게다가 유효시간과 시도 횟수는 코드 기본값으로만 존재하고 함수 환경변수에는 지정돼 있지 않아, 문서가 선언한 "설정은 전부 환경변수"와도 어긋난다.
- **근거**: 정책 값은 장기적으로 반드시 바뀐다. 클라이언트 맵이 이미 테넌트별 구조를 가지고 있으므로 자리를 하나 여는 비용이 거의 없다.
- **제안**: 클라이언트 맵의 테넌트 항목에 선택적 정책 오버라이드를 허용하고, 미지정 시 전역 기본값을 쓴다고 적는다. 전역 기본값 네 개는 환경변수로 노출한다.
- **Comment** (게시본)

```text
코드 정책 네 값이 모두 전역 상수입니다. 제품마다 다른 값을 요구하는 일은 흔한데, 예를 들어 어느 제품은 유효시간을 더 짧게 요구하고 메일 지연이 큰 지역에서는 더 길게 요구할 수 있습니다. 지금 구조로는 전 테넌트가 같은 값을 쓰게 됩니다.

클라이언트 맵이 이미 테넌트별 구조를 가지고 있으니 그 안에 선택적 정책 오버라이드 자리를 열고 미지정이면 전역 기본값을 쓴다고 적어 두시면 비용 없이 여지가 생깁니다.

관련해서 유효시간과 시도 횟수는 코드 기본값으로만 있고 함수 환경변수에는 지정돼 있지 않은 것 같습니다. 내부 설계 절이 설정은 전부 환경변수라고 선언하고 있어서 그 부분과 맞춰 주시면 좋겠습니다.
```

- **게시** · thread 87993 · 2026-09-01 23:01 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 테넌트별 정책 오버라이드는 결정기록 D-04에 '클라이언트 맵에 선택적 오버라이드 자리를 열고 미지정이면 전역 기본값'으로 등재했습니다(저비용, 요구 오면 반영). 또한 유효시간·시도 횟수가 코드 기본값으로만 있고 함수 환경변수엔 미지정이라 '설정은 전부 환경변수' 선언과 어긋나는 점을 §5에 명시하고 env화를 D-04에 넣었습니다. 코드 변경이라 별도 반영합니다.
- 조치: §1 '전역 상수' 명시 + §5 env 불일치 명시 + D-04 · 상태: **Resolved(fixed)**

## C-17 · docs/otp-mail-api-spec.md:17 · 🟡 · 저장 리전 선택과 규제 검토가 없다 · **미게시**

- **원문**
  > `| 계정 / 리전 | 032413839856 (prod) / us-east-1 |`
- **기준**: spec-standard.md §6.6 Design and Implementation Constraints(표준·법 준수) · abc-spec-review 체크리스트 M(법·규제 변화 대응)
- **문제**: 수신자 이메일 주소가 미국 리전의 DynamoDB와 CloudWatch에 저장되고 30일 남는다. 그 수신자는 베트남·이집트·브라질·호주 제품의 최종 사용자다. 리전을 이 계정으로 정한 근거는 적혀 있지만(SES 도메인 식별자와 프로덕션 권한이 이 계정에 있음), 개인정보가 국경을 넘는 것에 대한 검토는 문서에 없다.
- **근거**: 이 항목은 나중에 발견되면 비싸다. 특정 국가에서 현지 저장을 요구받으면 단일 계정·단일 테이블·단일 리전 전제가 통째로 흔들린다. 지금은 "검토했고 문제없다고 판단했다" 한 줄이면 끝나지만, 그 한 줄이 없으면 나중에 아무도 검토하지 않았다는 뜻이 된다.
- **제안**: 저장·로그에 남는 개인정보 항목(수신자 주소)과 저장 위치, 보존 기간, 관련 규제 검토 여부를 한 문단으로 적는다. 검토가 아직이면 미결에 담당자와 함께 넣는다.
- **Comment 초안**

```text
수신자 이메일 주소가 미국 리전의 테이블과 로그에 저장되고 30일 남습니다. 그 수신자는 베트남과 이집트와 브라질과 호주 제품의 최종 사용자입니다.

리전을 이 계정으로 정한 근거는 도메인 식별자와 발송 권한이 여기 있다는 것으로 적혀 있는데, 개인정보가 국경을 넘는 부분에 대한 검토는 문서에 없습니다.

이 항목은 나중에 발견되면 비싼 편입니다. 어느 국가에서든 현지 저장을 요구받으면 단일 계정 단일 테이블 전제가 통째로 흔들립니다. 지금은 저장하고 로그에 남는 개인정보 항목이 무엇이고 어디에 얼마나 남는지, 관련 규제를 검토했는지를 한 문단으로 적어 두시면 충분합니다. 검토가 아직이면 담당자와 함께 미결에 넣어 두는 편이 좋겠습니다.
```

- 조치: — · 상태: **미게시(선별 제외)**

---

# D. 보안

## C-18 · docs/otp-mail-api-spec.md:210 · 🔴 · 테넌트 전체 발송 총량 상한이 없다 · [thread 87994] · **Resolved(fixed)**

- **원문**
  > `pk = "{tenant}#{email 소문자}#{시간 윈도}"      # 윈도 = floor(now / 3600s)`
- **기준**: spec-standard.md §6.2 Security Requirements · abc-spec-review 체크리스트 L
- **문제**: 상한 키가 테넌트와 수신자와 시간 윈도의 조합이라 제한은 수신자 한 명당 시간당 10회다. 서로 다른 주소로 나가는 총량에는 제한이 없고, 실질 상한은 스테이지 스로틀 5 rps뿐이라 산술적으로 하루 43만 건까지 열려 있다.
- **근거**: 4장은 상한이 필요한 이유를 "월 20여 건만 보내는 계정이라 바운스 2건이면 임계를 넘는다"로 설명한다. 그 논리대로면 위험한 시나리오는 한 주소로의 반복 발송이 아니라 서로 다른 주소 수십 건에 대한 발송이다. 즉 현재 상한은 문서가 스스로 지목한 위험을 막지 못한다. 키가 유출되면 좁은 엔드포인트라도 계정 발송 정지를 유발할 수 있고, 정지되면 문서가 우려한 대로 다른 서비스의 메일까지 함께 멈춘다.
- **제안**: 테넌트 단위 총량 행(예: `quota#{tenant}#{윈도}`)을 같은 조건부 증가 방식으로 하나 더 둔다. 도입이 어렵다면 최소한 "수신자별 상한만 있고 총량 상한은 없다"를 명시하고 발송량·바운스율 경보 임계를 운영 규칙에 넣는다.
- **Comment** (게시본)

```text
발송 상한 키가 테넌트와 수신자와 시간 윈도 조합이라, 제한이 걸리는 것은 수신자 한 명당 시간당 10회입니다. 서로 다른 주소로 나가는 총량에는 제한이 없고 실질 상한은 스테이지 스로틀뿐이라 산술적으로는 하루 수십만 건까지 열려 있습니다.

운영 규칙 절에서 상한이 필요한 이유를 월 20여 건만 보내는 계정이라 바운스 2건이면 임계를 넘는다로 설명하고 계신데, 그 논리대로면 위험한 쪽은 한 주소로의 반복 발송이 아니라 서로 다른 주소 수십 건에 대한 발송입니다. 지금 상한은 그 시나리오를 막지 못합니다. 키가 유출되면 좁은 엔드포인트라도 계정 발송 정지를 유발할 수 있고, 정지되면 우려하신 대로 다른 서비스 메일까지 함께 멈춥니다.

테넌트 단위 총량 행을 같은 조건부 증가 방식으로 하나 더 두는 것이 가장 싸 보입니다. 도입이 어려우시면 최소한 수신자별 상한만 있고 총량 상한은 없다는 점을 문서에 적고, 발송량과 바운스율 경보 임계를 운영 규칙에 함께 넣어 주시면 좋겠습니다.
```

- **게시** · thread 87994 · 2026-09-01 23:03 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 가장 중요한 지적으로 봤습니다. 현재 상한이 수신자별뿐이라 서로 다른 주소 수십 건 발송이 열려 있고, 그게 바운스 위험의 실제 시나리오라는 점을 §5에 명시했습니다. 테넌트 단위 총량 행을 같은 조건부 증가 방식으로 추가하는 대응을 결정기록 D-02에 등재하고, 발송량 실측 또는 유출 시나리오 구체화 시 도입 검토로 다시 볼 조건을 달았습니다. 이미 apply된 저장 구조 변경이라 별도 결정으로 반영하겠습니다.
- 조치: §5 ⚠ 블록에 총량 공백 명시 + D-02 등재(**미구현**) · 상태: **Resolved(fixed)**

## C-19 · docs/otp-mail-api-spec.md:27 · 🔴 · 문서 조회 경로가 인증과 소스 IP 검사를 거치지 않는다 · [thread 87995] · **Resolved(fixed)**

- **원문**
  > `인증은 모든 호출 공통: \`x-api-key\` 헤더. 테넌트는 body가 아니라 **키에서 도출**된다`
- **기준**: abc-spec-review 체크리스트 D(섹션 간 일관성)·K(연계 산출물 정합성) · spec-standard.md §6.2
- **문제**: 구현에서 문서 경로는 키 확인과 허용목록 검사 이전에 처리되고 문서 플래그만 본다(`src/entrypoints/lambda.mjs:27-35`). 플래그가 켜져 있으면 인터넷의 누구나 열람할 수 있다. 스펙 첫 문단의 선언과 정면으로 어긋난다.
- **근거**: 계약 문서의 첫 선언과 실제 동작이 다른 것은 그 자체로 결함이다. 더해서 노출되는 화면은 시도 기능이 켜진 상태이고 외부 CDN 자산을 불러온다.
- **제안**: 인증 문장을 발급·검증 두 엔드포인트로 좁히고, 문서 경로가 무인증 공개임을 명시한다. 그 위에 플래그를 켜는 조건(누가, 얼마 동안, 왜)을 운영 규칙에 적는다. 상시 노출이 부담스러우면 문서 경로에도 허용목록만 적용하는 선택지가 있다.
- **Comment** (게시본)

```text
1장 첫 문장이 인증은 모든 호출 공통이라고 선언하는데, 구현에서 문서 경로는 키 확인과 소스 IP 검사 이전에 처리되고 문서 플래그만 봅니다. 플래그가 켜져 있으면 인터넷에서 누구나 열람할 수 있습니다.

계약 문서의 첫 선언과 실제 동작이 어긋나는 부분이라 정리가 필요해 보입니다. 노출되는 화면은 시도 기능이 켜져 있고 외부 CDN 자산도 불러옵니다.

인증 문장을 발급과 검증 두 엔드포인트로 좁히고 문서 경로는 무인증 공개임을 명시해 주시면 좋겠습니다. 그 위에 플래그를 켜는 조건을 운영 규칙에 한 줄 넣어 두시면 더 좋고, 상시 노출이 부담스러우시면 문서 경로에도 허용목록만 적용하는 선택지도 있습니다.
```

- **게시** · thread 87995 · 2026-09-01 23:03 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 00:39 · cid 2]**
  > 지적 감사합니다. 문서의 경우 플래그가 활성화 돼있어도 ip 검사하여 사무실 ip가 아니면 요청을 거부하도록 수정하였습니다.
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 3]**
  > 반영했습니다. 코드까지 고쳤습니다. §1에서 인증 문장을 발급·검증 두 엔드포인트로 좁히고 문서 경로는 무인증 공개임을 명시했습니다. 그리고 docsEnabled를 docsAccess(sourceIp)로 바꿔 시크릿의 선택적 docs_allow_ip로 /docs에 소스 IP 게이트를 걸 수 있게 했습니다(허용목록 없으면 기존대로 플래그만, 있으면 그 IP만 통과). 프로덕션 함수에 배포해 사무실 IP에서만 200, 그 외 404를 실측 확인했습니다. 커밋 6bbe439.
- 조치: **코드 수정** — docsEnabled→docsAccess(sourceIp), 시크릿 docs_allow_ip 게이트, 두 진입점·테스트 반영(6bbe439). §1 인증 문장 축소 · 상태: **Resolved(fixed)**

## C-20 · docs/otp-mail-api-spec.md:233 · 🔴 · 코드 해시에 페퍼가 없어 저장 방식이 사실상 평문과 같다 · [thread 87996] · **Resolved(fixed)**

- **원문**
  > `평문 코드는 어디에도 저장하지 않는다. 단 6자리 공간은 오프라인 브루트포스가`
- **기준**: spec-standard.md §6.2 Security Requirements
- **문제**: 6자리 숫자 공간은 100만 개라 사전 계산이 즉시 끝난다. 테이블 덤프를 가진 사람에게 이 해시는 사실상 평문이다. 문서는 그 사실을 인정하면서도 결론을 만료와 시도 캡으로만 맺는다.
- **근거**: 저장 방식을 바꾸는 것만으로 이 위험이 사라지므로 감수할 이유가 없다. 시크릿에 페퍼 값을 하나 추가하고 키가 있는 해시로 저장하면 성능 영향 없이 해결되고, "평문을 저장하지 않는다"는 문장도 그때 비로소 사실이 된다.
- **제안**: 시크릿에 페퍼를 추가하고 `HMAC-SHA256(페퍼, 테넌트+수신자+코드)` 형태로 저장한다. 페퍼 교체 절차도 한 줄 적는다(교체 시 진행 중인 코드는 무효).
- **Comment** (게시본)

```text
저장 방식에 대해 한 가지 있습니다. 문서에 평문 코드는 어디에도 저장하지 않는다고 되어 있는데, 6자리 숫자 공간은 100만 개라 사전 계산이 즉시 끝납니다. 테이블 덤프를 가진 사람에게 이 해시는 사실상 평문입니다.

바로 아래에서 오프라인 추측이 가능하다는 점을 이미 인정하고 계시는데, 결론이 만료와 시도 캡으로만 맺어져 있습니다. 저장 방식만 바꾸면 이 위험은 성능 영향 없이 사라집니다.

시크릿에 페퍼 값을 하나 추가하고 키가 있는 해시로 저장하시면 어떨까 합니다. 그러면 평문을 저장하지 않는다는 문장도 그때 비로소 사실이 됩니다. 페퍼를 교체하면 진행 중인 코드가 무효가 되니 그 절차도 한 줄 적어 두시면 좋겠습니다.
```

- **게시** · thread 87996 · 2026-09-01 23:04 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 6자리 공간은 사전 계산이 즉시 끝나 테이블 덤프 소유자에게 sha256 해시가 사실상 평문이라는 점, 인정합니다. §5에 이를 명시하고 실질 방어선이 해시가 아니라 만료+시도 캡임을 적었습니다. 시크릿에 페퍼를 추가한 keyed 해시로 바꾸면 '평문 미저장'이 비로소 사실이 된다는 대응을 결정기록 D-10에 등재하고, 페퍼 교체 시 진행 중 코드가 무효가 된다는 절차도 남겼습니다. 코드 변경이라 별도 반영합니다.
- 조치: §5에 해시=사실상 평문 명시 + D-10 등재(**미구현**) · 상태: **Resolved(fixed)**

## C-21 · docs/otp-mail-api-spec.md:240 · 🟡 · 발송 상한 저장소의 통과 허용 정책은 근거가 구조상 성립하지 않는다 · **미게시**

- **원문**
  > `| 발송 상한 카운터 | **fail open** (발송 진행) | 카운터 블립이 전 지역 로그인 장애가 되면 안 됨. 스로틀이 폭주 방지 |`
- **기준**: abc-spec-review 체크리스트 L(상충 요구·미정의 분기)
- **문제**: 카운터와 코드 레코드가 같은 테이블에 있다(200줄). 그 테이블이 불가용하면 바로 다음 단계인 코드 저장이 503을 내므로 발송은 어차피 진행되지 않는다. 통과 허용이 실제로 이득을 주는 구간은 카운터 쓰기만 실패하고 코드 쓰기는 성공하는 좁은 경우뿐인데, 하필 그 구간에서 발송 보호막만 사라진다.
- **근거**: 두 정책이 같은 자원 위에서 반대 방향을 향해 있어 문서에 적힌 의도가 실행되지 않는다. 설계 근거가 틀린 채로 남으면 나중에 읽는 사람이 잘못된 전제 위에서 수정한다.
- **제안**: 셋 중 하나다. 근거를 부분 실패 한정으로 다시 쓰거나, 카운터를 별도 저장소로 분리해 근거를 실제로 성립시키거나, 통과 허용 상태를 별도 신호로 남기고 그 상태에서 총량 경보를 걸어 공백을 메운다. 마지막 방향의 절반은 `f384cc8`이 이미 했다(통과 허용 진입 시 경보). 남는 것은 문서의 근거 정합과, 그 경보를 받았을 때 볼 총량 지표가 아직 없다는 점이다(C-32).
- **Comment 초안**

```text
장애 정책 표에서 상한 카운터를 통과 허용으로 두신 이유가 카운터 블립이 전 지역 로그인 장애가 되면 안 된다는 것인데, 카운터와 코드 레코드가 같은 테이블에 있습니다. 그 테이블이 불가용하면 바로 다음 단계인 코드 저장이 503을 내므로 발송은 어차피 진행되지 않습니다.

그러면 통과 허용이 실제로 이득을 주는 구간은 카운터 쓰기만 실패하고 코드 쓰기는 성공하는 좁은 경우뿐인데, 하필 그 구간에서 발송 보호막만 사라집니다. 정책 자체보다 근거가 구조와 맞지 않는 쪽이 신경 쓰입니다. 근거가 틀린 채로 남으면 나중에 읽는 사람이 잘못된 전제 위에서 고치게 됩니다.

근거를 부분 실패 한정으로 다시 쓰시거나, 카운터를 별도 저장소로 분리해 근거를 실제로 성립시키시거나, 통과 허용 상태를 별도 신호로 남기고 그 상태에서 총량 경보를 거는 방식 중 하나면 정리될 것 같습니다.
```

- 조치: — · 상태: **미게시(선별 제외)**

## C-22 · docs/otp-mail-api-spec.md:253 · 🟡 · 소스 IP를 어디서 얻는지가 이관 계약에 없다 · **미게시**

- **원문**
  > `| \`entrypoints/lambda.mjs\` | \`entrypoints/server.mjs\` (작성 완료, \`GET /healthz\`) | 컨테이너 CMD |`
- **기준**: abc-spec-review 체크리스트 K·M · spec-standard.md §6.2
- **문제**: 현재 경로는 게이트웨이가 채워 주는 값이라 신뢰할 수 있지만, 컨테이너 진입점은 설정에 따라 전달 헤더의 첫 항목을 쓴다(`src/entrypoints/server.mjs:32-38`). 앞단이 그 헤더를 정리하지 않으면 소스 IP 허용목록이 우회된다. 이관 계약 표에 이 항목이 없다.
- **근거**: 이 표는 이관 시 무엇이 갈리는지를 정리한 곳이고, 인증의 절반을 차지하는 값의 출처가 빠져 있으면 이관하는 사람이 기본값으로 두거나 반대로 무심코 켠다.
- **제안**: 표에 소스 IP 결정 방식 행을 추가하고, 전달 헤더를 신뢰하려면 앞단이 그 헤더를 반드시 덮어써야 한다는 전제를 명시한다.
- **Comment 초안**

```text
이관 계약 표에 소스 IP를 어디서 얻는지가 빠져 있습니다.

지금 경로는 게이트웨이가 채워 주는 값이라 신뢰할 수 있는데, 컨테이너 진입점은 설정에 따라 전달 헤더의 첫 항목을 씁니다. 앞단이 그 헤더를 정리하지 않으면 소스 IP 허용목록이 우회됩니다. 이 표가 이관 시 무엇이 갈리는지 정리한 곳인데 인증의 절반을 차지하는 값의 출처가 없으면, 이관하는 분이 기본값으로 두거나 반대로 무심코 켤 수 있습니다.

소스 IP 결정 방식 행을 하나 추가하시고, 전달 헤더를 신뢰하려면 앞단이 그 헤더를 반드시 덮어써야 한다는 전제를 함께 적어 주시면 좋겠습니다.
```

- 조치: — · 상태: **미게시(선별 제외)**

## C-23 · docs/otp-mail-api-spec.md:182 · 🟡 · 사무실 IP 허용에 종료 조건이 없다 · [thread 87997] · **Resolved(fixed)**

- **원문**
  > `**사무실 IP 허용은 위험 수용 결정이다** (2026-09-01, 초기 금지 번복). 엔드포인트가`
- **기준**: spec-standard.md §6.2 · abc-spec-review 체크리스트 J(재검토 조건)
- **문제**: 위험 수용으로 기록한 것은 좋지만 언제까지인지가 없다. 사무실 IP는 공유 출구라 그 사무실에서 나가는 누구나가 유출된 키를 쓸 수 있다는 뜻이고, 그 상태에서는 IP가 방어선 역할을 하지 못한다.
- **근거**: 미결 3번이 앱별 송신 주소를 수집 중이라고 하므로 이 허용은 그 수집이 끝날 때까지의 임시 조치로 보인다. 그런데 문서에는 임시라는 표시가 없어서, 수집이 끝나도 남는다.
- **제안**: 사무실 IP 항목에 종료 조건(해당 앱의 고정 송신 주소 확보 시 제거)을 적고, 미결 3번과 연결한다.
- **Comment** (게시본)

```text
사무실 IP 허용을 위험 수용으로 기록해 두신 것은 좋습니다. 다만 언제까지인지가 없습니다.

사무실 IP는 공유 출구라 그 사무실에서 나가는 누구나가 유출된 키를 쓸 수 있다는 뜻이고, 그 상태에서는 IP가 방어선 역할을 하지 못합니다. 미결 항목에 앱별 송신 주소를 수집 중이라고 되어 있어서 이 허용은 그때까지의 임시 조치로 읽히는데, 문서에 임시라는 표시가 없으면 수집이 끝나도 그대로 남습니다.

해당 앱의 고정 송신 주소가 확보되면 제거한다는 종료 조건을 붙이고 미결 항목과 연결해 주시면 좋겠습니다.
```

- **게시** · thread 87997 · 2026-09-01 23:05 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §2에 사무실 IP 허용은 dev,qa 환경만 가능함을 명시하고 prod 앱들은 각 앱들의 서버, NAT Gateway IP만을 허용하도록 수정했습니다.
- 조치: §2에서 사무실 IP를 develop 테넌트 전용으로 한정, prod 앱 키 배제(17955cd) · 상태: **Resolved(fixed)**

## C-24 · docs/otp-mail-api-spec.md:133 · 🟡 · 무중단 키 교체 절차가 없다 · [thread 87998] · **Resolved(fixed)**

- **원문**
  > `- **키**: 클라이언트별 발급 (\`openssl rand -hex 32\` 수준이면 충분).`
- **기준**: spec-standard.md §6.2 · §6.8 Operations
- **문제**: 유출 시 로테이션을 언급하지만 어떻게 무중단으로 바꾸는지가 없다. 맵 구조상 같은 테넌트로 키를 둘 두면 되는데 그게 허용되는지 문서가 답하지 않는다. 정기 교체 주기도 없다.
- **근거**: 절차가 없으면 실제 유출 시점에 즉흥적으로 처리하게 되고, 그때는 대개 서비스 중단을 감수한다.
- **제안**: 같은 테넌트에 키를 복수로 둘 수 있으며 교체는 신규 추가 후 구 키 제거 순서로 한다는 것을 명시한다. 정기 교체 주기를 둘지도 함께 정한다.
- **Comment** (게시본)

```text
키가 유출되면 로테이션한다고 적혀 있는데 어떻게 무중단으로 바꾸는지가 없습니다. 맵 구조상 같은 테넌트로 키를 둘 두면 될 것 같은데 그게 허용되는지 문서가 답하지 않습니다.

절차가 없으면 실제 유출 시점에 즉흥적으로 처리하게 되고, 그때는 대체로 중단을 감수하게 됩니다. 같은 테넌트에 키를 복수로 둘 수 있으며 교체는 신규 추가 후 구 키 제거 순서로 한다는 정도를 적어 주시면 좋겠습니다. 정기 교체 주기를 둘지도 함께 정해 두시면 더 좋겠습니다.
```

- **게시** · thread 87998 · 2026-09-01 23:05 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §2에 키 로테이션 절차를 적었습니다 — 같은 테넌트에 키를 복수로 둘 수 있으며, 교체는 신규 키 추가 → 앱 전환 → 구 키 제거 순서로 무중단으로 한다. 정기 교체 주기는 Dent, Lab, RC 앱에 각각 반영돼야 하고 키가 유출되더라도 발신자가 각 테넌트별 허용 ip가 아니면 이메일이 발송되지 않고 코드에서 막히기 때문에 보류했습니다.
- 조치: §2에 무중단 교체 절차 명시(정기 주기는 보류) · 상태: **Resolved(fixed)**

## C-25 · docs/otp-mail-api-spec.md:85 · 🟡 · 추측 예산의 근거가 없고, 활성 코드가 없는 상태의 검증 호출은 제한되지 않는다 · [thread 87999] · **Resolved(fixed)**

- **원문**
  > `| 오입력 허용 | 5회 (초과 시 폐기) |`
- **기준**: spec-standard.md §6.2 · abc-spec-review 체크리스트 L(경계 조건)
- **문제**: 왜 5회인지가 없다. 계산해 보면 발송 상한 10회와 곱해 수신자당 시간당 최대 50회 추측이고 100만 분의 1 공간에서는 충분히 안전한데, 그 계산이 문서에 없어 근거가 서지 않는다. 별개로, 시도 횟수는 활성 코드 레코드에만 누적되므로 활성 코드가 없는 상태의 검증 호출은 카운터를 전혀 소비하지 않는다.
- **근거**: 후자는 코드 추측 자체를 가능하게 하지는 않지만, 유출된 키로 검증을 계속 호출해 공용 스로틀을 소진시키면 다른 테넌트의 로그인이 함께 막힌다(C-06과 같은 뿌리).
- **제안**: 시도 예산 계산을 한 줄 적는다. 활성 코드 없는 검증 호출이 제한되지 않는다는 사실도 함께 적고, 격리가 필요하다고 판단되면 수신자 단위의 가벼운 시도 상한을 코드 존재 여부와 무관하게 둔다.
- **Comment** (게시본)

```text
오입력 허용이 5회인 근거가 문서에 없습니다. 계산해 보면 발송 상한 10회와 곱해 수신자당 시간당 최대 50회 추측이고 100만 분의 1 공간이라 충분히 안전한데, 이 계산을 한 줄 적어 두시면 왜 5회인가에 대한 답이 문서 안에서 끝납니다.

별개로, 시도 횟수는 활성 코드 레코드에만 누적되므로 활성 코드가 없는 상태의 검증 호출은 카운터를 전혀 소비하지 않습니다. 코드 추측 자체는 유효한 코드가 살아 있어야 가능하니 위험도는 낮습니다만, 유출된 키로 검증을 계속 호출해 공용 스로틀을 소진시키면 다른 테넌트의 로그인이 함께 막힐 수 있습니다. 이 점을 문서에 적어 두시거나, 격리가 필요하다고 보시면 수신자 단위의 가벼운 시도 상한을 코드 존재 여부와 무관하게 두는 방법이 있습니다.
```

- **게시** · thread 87999 · 2026-09-01 23:06 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §1에 오입력 5회 근거를 적었습니다(발송 상한 10 × 오입력 5 = 시간당 최대 50회 추측, 100만 공간 대비 안전). 또한 시도 카운터가 활성 코드 레코드에만 누적되어 활성 코드 없는 검증 호출은 카운터를 소비하지 않는다는 점, 그리고 유출 키로 검증을 계속 호출해 공용 스로틀을 소진시키면 다른 테넌트 로그인이 함께 막힐 수 있다는 점을 §1·§5·결정기록 D-06에 남겼습니다.
- 조치: §1에 시도 예산 계산 + 스로틀 공유 명시 · 상태: **Resolved(fixed)**

## C-26 · docs/otp-mail-api-spec.md:70 · 🟢 · 보안 속성을 명시적으로 적어 두면 좋을 것 두 가지 · **미게시**

- **원문**
  > `| reason | 의미 | 앱이 할 일 |`
- **기준**: spec-standard.md §6.2
- **문제**: (가) 검증 사유가 세분화돼 있어 유효한 키를 가진 호출자는 임의 주소에 활성 코드가 있는지 알 수 있다. 반대로 발급은 주소 존재 여부와 무관하게 같은 응답을 주므로 계정 열거는 막혀 있는데, 이 장점이 문서에 근거로 적혀 있지 않다. (나) 해시 비교가 상수 시간이 아니다. 해시끼리 비교라 실익은 거의 없지만 OTP 스펙에서는 통상 명시한다.
- **제안**: 테넌트가 키에서 도출되어 남의 코드 상태는 볼 수 없다는 점과 발급 응답이 주소 존재 여부를 노출하지 않는다는 점을 보안 속성으로 적고, 사유 세분화는 그 위에서 수용한 절충으로 기록한다. 비교 방식은 위협 모델 밖이라고 한 줄 남긴다.
- **Comment 초안**

```text
보안 속성으로 적어 두면 좋을 것 두 가지입니다.

하나는 검증 사유가 세분화돼 있어 유효한 키를 가진 호출자는 임의 주소에 활성 코드가 있는지 알 수 있다는 점입니다. 반대로 발급은 주소 존재 여부와 무관하게 같은 응답을 주므로 계정 열거는 막혀 있는데, 이건 이 설계의 장점인데도 문서에 근거로 적혀 있지 않습니다. 테넌트가 키에서 도출되어 남의 코드 상태는 볼 수 없다는 점과 함께 적어 두시고, 사유 세분화는 그 위에서 수용한 절충으로 기록하시면 좋겠습니다.

다른 하나는 해시 비교가 상수 시간이 아니라는 점입니다. 해시끼리 비교라 실익이 거의 없는 지적입니다만, 위협 모델 밖이라고 한 줄 적어 두면 다음에 보는 사람이 같은 질문을 반복하지 않습니다.
```

- 조치: — · 상태: **미게시(선별 제외)**

---

# E. 계약 정확성

## C-27 · docs/otp-mail-api-spec.md:95 · 🟡 · 429의 의미가 두 가지이고 재시도 시점이 계약에 없다 · [thread 88000] · **Resolved(fixed)**

- **원문**
  > `| 429 | 발급 한정: 수신자별 발송 상한(10/h) 초과 |`
- **기준**: spec-standard.md "API 스키마 완전성 규칙" · abc-spec-review 체크리스트 F·L
- **문제**: 표는 429를 발급 한정으로 정의하지만, 스테이지 스로틀도 429를 돌려주고 이쪽은 검증 호출에도 걸린다. 두 429는 재시도 전략이 정반대인데(상한 초과는 최대 한 시간 대기, 스로틀은 수백 밀리초 뒤 재시도) 구분할 수단이 정의돼 있지 않다. 더해서 상한은 고정 한 시간 버킷이라 재시도 가능 시점이 다음 정시인데, 응답에 남은 시간이 없고 문서에도 리셋 시점이 없다.
- **제안**: 표에 스로틀 429를 별도 행으로 넣고 응답 본문으로 구분 가능하게 정의한다. 429 응답에 남은 초를 담거나, 최소한 정시 경계에 리셋된다는 사실을 명시한다.
- **Comment** (게시본)

```text
에러 표에서 429를 발급 한정 수신자별 상한 초과로 정의하고 계신데, 스테이지 스로틀도 429를 돌려주고 이쪽은 검증 호출에도 걸립니다. 두 429는 재시도 전략이 정반대라서 앱이 구분할 수 있어야 합니다. 상한 초과는 최대 한 시간을 기다려야 하고 스로틀은 수백 밀리초 뒤 재시도하면 됩니다.

함께, 상한이 고정 한 시간 버킷이라 재시도 가능 시점이 다음 정시인데 응답에 남은 시간을 알려주는 값도 없고 문서에도 리셋 시점이 없습니다. 429를 받은 앱은 무한정 재시도하거나 임의로 기다리는 수밖에 없습니다.

표에 스로틀 429를 별도 행으로 넣고 본문으로 구분되게 정의해 주시고, 응답에 남은 초를 담거나 최소한 정시 경계에 리셋된다는 점을 적어 주시면 좋겠습니다.
```

- **게시** · thread 88000 · 2026-09-01 23:06 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. 에러 표에 상한 429와 스로틀 429를 별도 행으로 나누고 재시도 전략이 정반대임을 적었습니다(상한은 최대 1시간 대기, 스로틀은 수백 ms 후 재시도, 스로틀은 검증에도 걸림). 상한 429가 고정 1시간 버킷이라 정시 경계에 리셋된다는 점도 명시했습니다. Retry-After 헤더로 남은 초를 응답에 담는 것은 미결로 두었습니다.
- 조치: 에러 표를 상한/스로틀 429 2행 분리 + 재시도 열 + 정시 리셋 명시 · 상태: **Resolved(fixed)**

## C-28 · docs/otp-mail-api-spec.md:47 · 🟡 · 멱등성이 없고 클라이언트 재시도 규칙이 없다 · [thread 88001] · **Resolved(fixed)**

- **원문**
  > `**재전송 = 이 엔드포인트를 다시 호출.** 수신자당 활성 코드는 1개라, 재호출하면`
- **기준**: abc-spec-review 체크리스트 L(경계·동시성)
- **문제**: 재호출이 곧 재전송이므로, 앱이 타임아웃 후 자동 재시도하면 메일 두 통이 나가고 첫 코드는 죽고 상한은 2회 소모된다. 사용자에게는 먼저 도착한 코드가 안 먹는 형태로 나타난다. 설계 자체는 의도된 것이므로 문제는 클라이언트 규칙의 부재다.
- **제안**: 타임아웃이나 5xx에 대해 발급을 자동 재시도하지 않고 재전송은 사용자의 명시적 요청으로만 호출한다는 규칙을 계약에 넣는다. 자동 재시도가 불가피하면 멱등성 키 도입을 검토한다.
- **Comment** (게시본)

```text
재호출이 곧 재전송인 설계는 이해했습니다. 다만 그래서 앱이 타임아웃 후 자동 재시도를 하면 메일 두 통이 나가고 첫 코드는 죽고 상한은 2회 소모됩니다. 사용자에게는 먼저 도착한 코드가 안 먹는 형태로 보입니다.

설계는 의도한 것이니 클라이언트 쪽 규칙을 계약에 넣어 주시면 좋겠습니다. 타임아웃이나 5xx에 대해 발급을 자동 재시도하지 않고 재전송은 사용자의 명시적 요청으로만 호출한다는 문장이면 충분합니다. 자동 재시도가 불가피한 사정이 있으면 멱등성 키를 검토하는 방법도 있습니다.
```

- **게시** · thread 88001 · 2026-09-01 23:07 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. §1에 클라이언트 재시도 규칙을 계약으로 넣었습니다 — 타임아웃·5xx에 발급을 자동 재시도하지 않고, 재전송은 사용자의 명시적 요청으로만 호출한다. 자동 재시도가 불가피하면 멱등성 키 도입을 검토한다는 점도 남겼습니다.
- 조치: §1에 클라이언트 재시도 규칙을 계약으로 명시 · 상태: **Resolved(fixed)**

## C-29 · docs/otp-mail-api-spec.md:96 · 🟡 · 발송 실패 시 직전 코드까지 함께 무효가 된다 · [thread 88002] · **Resolved(fixed)**

- **원문**
  > `| 502 | 발급 한정: SES 발송 실패 |`
- **기준**: abc-spec-review 체크리스트 L(에러 경로) · spec-philosophy §3.6("에러 케이스만 빠진 스펙은 절반만 적은 것")
- **문제**: 저장이 발송보다 먼저이므로, 재전송을 눌렀는데 발송이 실패하면 사용자는 새 코드를 받지 못한 채 직전에 받은 유효한 코드까지 잃는다. 되돌리는 처리가 없고 발송 상한도 이미 1회 소모된 상태다. 이 시퀀스가 문서에 없다.
- **제안**: 발송 실패 시 방금 저장한 레코드를 지우는 편이 낫다. 그게 아니면 502를 받으면 이전 코드도 무효라는 점과 상한이 소모됐다는 점을 명시해 앱이 재발급을 안내할 근거를 준다.
- **Comment** (게시본)

```text
저장을 발송보다 먼저 하는 순서 자체는 옳다고 봅니다. 다만 그 부작용이 문서에 없습니다.

재전송을 눌렀는데 발송이 실패하면 사용자는 새 코드를 받지 못한 채 직전에 받았던 유효한 코드까지 잃습니다. 되돌리는 처리가 없고 발송 상한도 이미 1회 소모된 상태입니다. 앱 팀이 502를 받았을 때 사용자에게 뭐라고 안내할지 결정할 근거가 없는 상황입니다.

발송이 실패하면 방금 저장한 레코드를 지우는 편이 나아 보입니다. 그렇게 하지 않으신다면 502를 받으면 이전 코드도 무효이고 상한도 한 번 소모됐다는 점을 문서에 적어 주시면 좋겠습니다.
```

- **게시** · thread 88002 · 2026-09-01 23:07 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 저장을 발송보다 먼저 하는 순서의 부작용을 §1 에러 표(502 행)와 §5에 명시했습니다 — 502 시 사용자는 새 코드를 못 받은 채 직전 유효 코드까지 잃고 상한도 1회 소모된 상태. 말씀대로 발송 실패 시 방금 저장한 레코드를 지우는 편이 낫다고 보아, 그 개선을 결정기록 D-09에 등재했습니다. 현재는 미구현이라 부작용을 문서에 남기는 것으로 두었고 코드 반영은 별도로 하겠습니다.
- **[임건혁(Jack) · 2026-09-02 02:05 · cid 3]**
  > 후속: 코드까지 반영했습니다. issue()에서 코드 저장 후 발송이 실패하면 방금 저장한 레코드를 best-effort 삭제하도록 구현했습니다 — 못 받은 코드가 슬롯에 남아 이후 검증에 mismatch로 답하는 대신 not_found가 되게 합니다. 단 이 재발급이 덮어쓴 직전 코드 복원과 상한 환불은 하지 않으며(문서에 명시), 삭제가 실패해도 502를 반환합니다. 유닛 테스트 2개 추가(정상 삭제로 not_found 확인 / remove가 던져도 502). 결정기록 D-09를 '구현됨'으로 갱신하고 스펙 502 행·§5·앱 README도 정합했습니다.
- 조치: **코드 구현** — 발송 실패 시 codeStore.remove best-effort + 유닛 테스트 2건(67dec10) · 상태: **Resolved(fixed)**

## C-30 · docs/otp-mail-api-spec.md:42 · 🟡 · 입력 처리 규칙이 문서마다 다르고 일부 응답이 표에 없다 · [thread 88003] · **Resolved(fixed)**

- **원문**
  > `| 필드 | 필수 | 설명 |`
- **기준**: abc-spec-review 체크리스트 F·K(연계 산출물 정합성)
- **문제**: 세 가지가 걸린다. (가) 언어 필드는 표에 두 값만 적혀 있으나 구현은 한국어가 아닌 값을 모두 영어로 강등하고 오류를 내지 않는다. 코드 안 OpenAPI 정의에는 이 폴백이 적혀 있는데 스펙 본문에는 없다. (나) 정의되지 않은 필드를 OpenAPI 정의는 거부한다고 선언하지만 서버는 무시하고 통과시킨다. (다) 존재하지 않는 경로와 문서 플래그가 꺼진 상태의 문서 경로가 모두 404를 내는데 에러 표에 없다.
- **근거**: 계약을 서술하는 문서가 셋인데 셋이 서로 다른 말을 하고 있다(C-35와 같은 뿌리).
- **제안**: 스펙 본문에 언어 폴백과 미지 필드 무시를 명시하고 OpenAPI 정의를 거기에 맞춘다. 에러 표에 404 행을 추가하고, 라우트를 명시적으로만 열고 포괄 라우트를 두지 않는다는 원칙도 계약으로 적는다.
- **Comment** (게시본)

```text
입력 처리 규칙에서 문서마다 말이 다른 부분이 있습니다.

언어 필드는 표에 두 값만 적혀 있는데 구현은 한국어가 아닌 값을 모두 영어로 강등하고 오류를 내지 않습니다. 코드 안 OpenAPI 정의에는 이 폴백이 적혀 있는데 스펙 본문에는 없습니다. 정의되지 않은 필드도 OpenAPI 정의는 거부한다고 선언하지만 서버는 무시하고 통과시킵니다.

그리고 존재하지 않는 경로와 문서 플래그가 꺼진 상태의 문서 경로가 모두 404를 내는데 에러 표에 그 행이 없습니다.

스펙 본문에 언어 폴백과 미지 필드 무시를 적고 OpenAPI 정의를 거기에 맞춰 주시면 좋겠습니다. 에러 표에 404를 추가하시면서, 라우트는 명시적으로만 열고 포괄 라우트를 두지 않는다는 원칙도 함께 적어 두시면 좋겠습니다. 지금 진입점이 검증 경로가 아닌 모든 요청을 발급으로 보내는 구조라, 나중에 포괄 라우트가 추가되면 임의 경로가 조용히 발송을 유발합니다. 지금은 라우트가 명시적이라 안전한데 그 안전이 문서에 근거로 남아 있지 않습니다.
```

- **게시** · thread 88003 · 2026-09-01 23:08 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 01:33 · cid 2]**
  > 반영했습니다. 네 가지 모두 처리했습니다. (1) 언어 폴백: §1 locale 행에 'ko 아니면 en으로 폴백, 오류 없음' 명시. (2) 미지 필드: §1 입력 처리 공통 규칙에 '모르는 필드는 무시하고 통과(forward-compat)' 명시하고, OpenAPI를 additionalProperties false→true로 실동작에 맞췄습니다. (3) 404: 에러 표에 '없는 경로 + DOC_ENABLED off 상태의 /docs* 경로' 행 추가. (4) 명시 라우트 원칙: §1에 'catch-all 없음, 엔트리포인트가 검증 아닌 모든 요청을 발급으로 보내는 구조라 catch-all이 생기면 임의 경로가 조용히 발송을 유발한다'를 근거로 남겼습니다(결정기록 D-05).
- 조치: 언어 폴백·미지 필드 무시 명시, OpenAPI additionalProperties true, 404 행, D-05 · 상태: **Resolved(fixed)**

## C-31 · docs/otp-mail-api-spec.md:76 · 🟢 · 엣지 케이스 서술 세 가지 · **미게시**

- **원문**
  > `| \`too_many_attempts\` | 오입력 5회 누적, 코드 폐기 | 재발급 유도 |`
- **문제**: (가) 시도 초과는 레코드가 삭제되므로 그 응답 한 번만 관측되고 이후에는 모두 발급된 적 없음으로 바뀐다. 사용 완료 표시도 레코드 수명이 다하면 마찬가지다. 표는 이 사유들이 안정적으로 유지되는 것처럼 읽힌다. (나) 같은 수신자에 대한 동시 발급 두 건은 마지막 쓰기만 남아 먼저 도착한 메일의 코드가 죽는다. (다) 발송 상한을 먼저 소모한 뒤 코드 저장에 실패해 503을 반환하는 경로에서는 예산만 깎이고 아무 일도 일어나지 않는다.
- **제안**: 각각 한 줄씩 표 아래에 덧붙인다.
- **Comment 초안**

```text
엣지 케이스 세 가지가 문서에 없어 보여 적어 둡니다. 셋 다 한 줄씩이면 해소될 것 같습니다.

시도 초과는 레코드가 삭제되므로 그 응답 한 번만 관측되고 이후에는 모두 발급된 적 없음으로 바뀝니다. 사용 완료 표시도 레코드 수명이 다하면 같습니다. 표만 보면 이 사유들이 계속 유지되는 것처럼 읽힙니다. 앱의 처리는 어느 쪽이든 재발급 유도로 같으니 표 아래 한 줄이면 충분합니다.

같은 수신자에 대해 발급이 거의 동시에 두 건 들어오면 마지막 쓰기만 남아 먼저 도착한 메일의 코드가 죽습니다. 동작 자체는 자연스러운데 사용자에게 보이는 증상이라 적어 두시면 좋겠습니다.

발송 상한을 먼저 소모한 뒤 코드 저장에 실패해 503을 반환하는 경로에서는 예산만 깎이고 아무 일도 일어나지 않습니다. 빈도는 낮겠지만 처리 순서 아래에 한 줄 있으면 나중에 문의가 줄 것 같습니다.
```

- 조치: — · 상태: **미게시(선별 제외)**

---

# F. 운영·개인정보·감사

## C-32 · docs/otp-mail-api-spec.md:174 · 🟡 · 바운스 경보는 생겼으나 대응 절차가 실제로 수행되지 않는다 — **총평(thread 87977)에 흡수 게시**

> **경과**: 초안 작성 중 `f384cc8`(CloudWatch 알람 모듈 신설 + OTP API 알람 배선)이 push되어, "바운스를 핵심 위험으로 지목하면서 감지 경로가 없다"는 원래 지적은 해소됐다. 아래는 그 위에 남는 부분이다.

- **원문**
  > (172~175줄) `- 장애 신호 3종 … 전부 loud 로그이며, **CloudWatch 알람으로 Teams infra 채널에 발송된다** (modules/cloudwatch-alarms). SES 계정 바운스/컴플레인율도 정지 임계의 절반(2.5% / 0.05%)에서 알람.`
  > (177~180줄) `**발송 테스트는 mailbox simulator로만** ... 실제 오타 주소로 바운스를 내면 5% 임계로 계정 발송이 정지될 수 있다.`
- **기준**: spec-standard.md §6.8 Operations · abc-spec-review 체크리스트 L(에러 경로)
- **문제**: 세 가지가 남는다.
  1. **대응 절차가 성립하지 않는다.** 새 알람의 설명은 바운스 발생 시 "otp-mail-api 로그에서 바운스를 낸 수신자와 테넌트를 찾아 억제하라"고 지시하는데(`monitoring.tf:42`), 함수 로그는 *발송*만 기록하고 바운스는 기록하지 않는다. 발송 이벤트를 구독하지 않으므로 어느 주소가 반송됐는지 알 방법이 없고, 발송 건수가 적을 때만 소거법으로 추정 가능하다. 계정 평판 지표는 "무언가 잘못됐다"만 알려 준다.
  2. **발송 실패에 경보가 없다.** 502를 만드는 경로는 코드가 예외를 잡아 정상 응답으로 바꾸므로 함수 오류 지표에 잡히지 않는다. 즉 메일이 계속 안 나가도 아무도 모른다.
  3. **발송 총량에 경보가 없다.** C-18의 상한 공백을 운영 쪽에서 메우려면 총량 급증이 경보 대상이어야 하는데 대상에 없다.
- **제안**: 발송 이벤트 대상을 구독해 반송·불만 이벤트를 개별로 수집하면 1번과 3번이 함께 해결된다. 여의치 않으면 최소한 알람 설명의 대응 절차를 실제 수행 가능한 것으로 고치고, 발송 실패와 발송 총량을 경보 대상에 추가한다.
- **게시** · 별도 스레드 없이 총평(thread 87977) 본문에 흡수
- 조치: 알람 구성 무변경 — 잔여를 C-46으로 재게시(thread 88243) · 상태: **이관됨 → C-46**

## C-33 · docs/otp-mail-api-spec.md:164 · 🟡 · 로그에 수신자 주소가 원문으로 남는다 · **미게시**

- **원문**
  > `{"msg":"otp issued",  "tenant":"dent-vn", "email":"...", "messageId":"..."}`
- **기준**: spec-standard.md §6.2 · abc-spec-review 체크리스트 M(개인정보 보존 정책)
- **문제**: 발급과 검증 로그 모두 수신자 주소를 그대로 남기고 30일 보관한다. 문서는 이것을 감사 추적의 정본으로 규정하면서 개인정보 관점의 판단은 남기지 않았다.
- **근거**: 이 주소들은 각 제품의 최종 사용자 계정이고, 저장 위치는 미국 리전이다(C-17). 감사 목적이 정당하더라도 최소 노출 원칙과의 형량이 문서에 기록돼 있어야 나중에 방어할 수 있다. 새 알람의 대응 절차가 로그에서 수신자를 찾도록 지시하고 있어서, 마스킹을 택할 경우 그 절차와 함께 조정해야 한다(C-32).
- **제안**: 마스킹한 형태나 테넌트 스코프 해시로 남기고, 원문이 필요한 경우의 근거와 접근 통제를 적는다. 원문 유지를 택한다면 그것을 명시적 결정으로 기록한다.
- **Comment 초안**

```text
발급과 검증 로그 모두 수신자 주소를 원문으로 남기고 30일 보관합니다. 이 로그를 감사 추적의 정본으로 규정하고 계신데 개인정보 관점의 판단은 문서에 없습니다.

이 주소들은 각 제품의 최종 사용자 계정이고 저장 위치는 미국 리전입니다. 감사 목적이 정당하더라도 최소 노출 원칙과의 형량이 문서에 기록돼 있어야 나중에 설명하기 쉽습니다.

마스킹한 형태나 테넌트 범위의 해시로 남기시는 쪽을 권하고, 원문이 필요한 경우의 근거와 접근 통제를 함께 적어 주시면 좋겠습니다. 원문 유지를 택하신다면 그것을 명시적 결정으로 기록해 두시는 것만으로도 충분합니다. 다만 이번에 추가하신 바운스 알람의 대응 절차가 로그에서 수신자를 찾도록 되어 있어서, 마스킹을 택하시면 그 절차와 함께 조정이 필요해 보입니다.
```

- 조치: — · 상태: **미게시(선별 제외)**

## C-34 · docs/otp-mail-api-spec.md:161 · 🟡 · 감사 로그를 액세스 로그와 이을 수 없고, 보존 30일의 근거가 없다 · **미게시**

> **경과**: 경보 수신처가 없다는 원래 지적은 `f384cc8`(Teams infra 채널)로 해소됐다. 아래는 그 위에 남는 부분이다.

- **원문**
  > `**감사 추적의 정본은 Lambda 로그다** (30일 보존). 모든 라인에 tenant가 찍힌다:`
- **기준**: spec-standard.md §6.8 Operations · §6.3.4 Reliability
- **문제**: (가) 함수 로그에 요청 식별자가 없어 게이트웨이 액세스 로그(요청 식별자·소스 IP·상태)와 이어 붙일 수 없다. 감사 정본이 함수 로그라면 이 연결은 필수다. (나) 보존을 30일로 정한 근거가 없다. OTP 오남용은 30일보다 늦게 발견되는 경우가 흔하다.
- **제안**: 모든 로그 라인에 요청 식별자를 필수 필드로 추가한다. 30일 판단 근거를 남기거나 각 제품의 규정 요구를 확인해 상향한다.
- **Comment 초안**

```text
감사 로그 쪽에 두 가지가 남아 보입니다.

함수 로그에 요청 식별자가 없어서 게이트웨이 액세스 로그와 이어 붙일 수 없습니다. 액세스 로그에는 요청 식별자와 소스 IP와 상태가 있고 함수 로그에는 테넌트와 수신자가 있는데, 둘을 잇는 값이 없습니다. 감사 정본이 함수 로그라면 이 연결은 필요해 보입니다.

그리고 보존을 30일로 정한 근거가 없습니다. OTP 오남용은 그보다 늦게 발견되는 경우가 흔해서, 근거를 한 줄 남기시거나 각 제품의 규정 요구를 확인해 상향하시는 편이 좋겠습니다.
```

- 조치: — · 상태: **미게시(선별 제외)**

---

# G. 문서 정합

## C-35 · docs/otp-mail-api-spec.md:21 · 🟡 · 계약 정본이 세 곳이고 버전 정책이 없다 · **미게시**

- **원문**
  > `| 버전 | 2026-09-01 v2 (B안) |`
- **기준**: spec-standard.md §1.5 Related Documents("같은 내용 복사 금지, 한 곳 + 링크") · abc-spec-review 체크리스트 K
- **문제**: 이 PR 설명은 스펙 문서를 별도 도구로 관리한다고 밝히는데 같은 PR에 문서가 들어오고, 코드 안에도 OpenAPI 정의가 있다. 계약을 서술하는 곳이 셋이고 이미 서로 다른 말을 하고 있다(C-30). 여기에 문서 헤더는 날짜와 v2를, OpenAPI 정의는 2.0.0을 쓰는데 무엇이 계약 버전이고 계약이 바뀔 때 무엇을 올리는지가 없다.
- **제안**: 레포 안의 스펙 문서를 정본으로 선언하고 OpenAPI 정의는 그로부터 파생, 외부 사본은 읽기용으로 규정하는 문장을 문서 머리에 둔다. 계약 버전을 무엇으로 하고 언제 올리는지도 한 줄 정한다.
- **Comment 초안**

```text
계약을 서술하는 곳이 셋입니다. 이 PR 설명은 스펙 문서를 별도 도구로 관리한다고 밝히는데 같은 PR에 문서가 들어오고, 코드 안에도 OpenAPI 정의가 있습니다. 이미 언어 폴백과 미지 필드 처리에서 셋이 서로 다른 말을 하고 있어서 시간이 지나면 더 갈라질 것 같습니다.

레포 안의 스펙 문서를 정본으로 선언하고 OpenAPI 정의는 거기서 파생된 것으로, 외부 사본은 읽기용으로 규정하는 문장을 문서 머리에 두시면 좋겠습니다.

겸해서 문서 헤더는 날짜와 v2를 쓰고 OpenAPI 정의는 2.0.0을 쓰는데, 무엇이 계약 버전이고 계약이 바뀔 때 무엇을 올리는지가 없습니다. 경로에 v1이 들어 있는 만큼 무엇을 깨는 변경으로 볼지와 그때 어떻게 병행 운영할지도 한 줄 있으면 좋겠습니다.
```

- 조치: — · 상태: **미게시(선별 제외)**

## C-36 · docs/otp-mail-api-spec.md:20 · 🟢 · 문서 안에서 엇갈리는 서술 두 가지 · **미게시**

- **원문**
  > `| Base URL | \`https://api.otp.cleverspacecloud.com\` |`
- **문제**: (가) 상단 표는 기준 주소를 확정값으로 적고, 미결 표는 같은 항목을 "확정, 적용 후 활성화"로 적는다. 지금 호출 가능한지 판단이 어렵다. (나) 내부 설계 절은 설정이 전부 환경변수라고 선언하는데 유효시간과 시도 횟수는 함수 환경변수에 없다(C-16과 연결).
- **제안**: 상단 표에 활성화 전임을 표시하거나 적용 완료 후 미결 항목을 닫는다. 환경변수 선언은 실제와 맞춘다.
- **Comment 초안**

```text
문서 안에서 엇갈리는 서술 두 가지입니다.

상단 표는 기준 주소를 확정값으로 적고 미결 표는 같은 항목을 확정이지만 적용 후 활성화로 적고 있어서, 지금 호출 가능한 상태인지 판단이 어렵습니다. 상단 표에 활성화 전임을 표시하시거나 적용이 끝난 뒤 미결 항목을 닫아 주시면 정리될 것 같습니다.

내부 설계 절 마지막에 설정은 전부 환경변수라고 되어 있는데, 유효시간과 시도 횟수는 함수 환경변수에 지정돼 있지 않고 코드 기본값으로만 있습니다. 둘 중 한쪽에 맞춰 주시면 좋겠습니다.
```

- 조치: — · 상태: **미게시(선별 제외)**

---

## 잘 되어 있어 유지할 것 (총평에 반영)

- 발송 전에 저장한다는 순서와 그 이유가 명시돼 있다. 검증 불가능한 코드가 메일로 나가지 않는다.
- 소비 마킹을 조건부 쓰기 한 번으로 처리해 동시 검증에서 정확히 하나만 성공한다. RDB 대응식까지 적어 둬서 이관 시에도 의미가 보존된다.
- 만료 판정을 저장소 수명 관리에 맡기지 않고 코드가 직접 한다는 점과 그 이유(삭제 지연)가 적혀 있다.
- 스토어별로 장애 방향이 다르다는 것을 표로 명시하고 각각의 이유를 붙였다. C-21은 근거의 정합성에 대한 지적이지 이 서술 방식에 대한 것이 아니다.
- 검증 실패를 전송 오류가 아니라 정상 응답으로 돌려주는 결정과 그 이유가 분명하다.
- 테넌트를 요청 본문이 아니라 키에서 도출한다는 결정과 그 이유가 적혀 있다.
- 확장 요구가 오면 구현하지 말고 되물으라는 하드 제약 문단은 이런 종류의 서비스가 가장 자주 무너지는 지점을 정확히 막고 있다.
- 포트와 어댑터 분리, 이관 시 갈리는 지점을 표로 정리한 것은 이 규모의 인프라 모듈에서 흔치 않은 수준이다.

---

## 부록: 스펙 서술 대 구현 대조

| 스펙 서술 | 대조 결과 |
| --- | --- |
| 6자리 균등 난수 | 일치(편향 없는 난수 함수 사용) |
| 유효시간 600초, 시도 5회 | 코드 기본값 일치. 단 함수 환경변수 미지정(C-16·C-36) |
| 수신자당 활성 코드 1개 | 일치(무조건 덮어쓰기) |
| 검증 실패는 200 응답 | 일치 |
| 상한 10회/시간, 고정 윈도 | 일치 |
| 상한 카운터 통과 허용, 코드 스토어 503 | 일치. 단 근거 미성립(C-21) |
| 인증은 모든 호출 공통 | **불일치**(C-19) |
| 429는 발급 한정 | **불일치**(C-27) |
| locale은 ko 또는 en | **부분 불일치**(C-30) |
| 미지 필드 거부(OpenAPI 정의) | **불일치** — 서버는 무시(C-30) |
| 평문 코드 미저장 | 형식상 일치, 실질은 C-20 |
| 코드는 로그에 남지 않음 | 일치 |
| 발신 주소 고정과 이중 잠금 | 일치 |
| 설정은 전부 환경변수 | **부분 불일치**(C-36) |

---

# Round 2 — Jack 반영본 재검토 (2026-09-02)

> **대상**: 헤드 `67dec10`(f384cc8 이후 5커밋 — `efd9018` 문서 대개편 / `6bbe439` /docs 소스 IP 게이트 / `7a1c6f9` 주석 정합 / `17955cd` 사무실 IP 정책 정정 / `67dec10` 502 시 레코드 삭제). 스펙 문서 267→450줄.
> **Jack 회신**: 27개 스레드 전건 회신(2026-09-02T01:33 일괄, 87995·88002는 후속 코멘트 추가).
> **처리**: 문서·코드 실물 대조 후 **21건 Resolve(fixed)**, **6건 Active 유지**. 아래 줄 번호는 `67dec10` 기준.

## 스레드 처리 결과

| 상태 | thread | 항목 | 판단 근거 |
| --- | --- | --- | --- |
| **fixed** | 87978~87982 | C-01~C-05 | §0 개요 신설 확인 — 목적(§0.1)·범위와 비목표 표(§0.2)·사용자 계층과 책임 표(§0.3)·보증 범위(§0.4)·종속성 표(§0.5) 모두 실재 |
| **fixed** | 87985 | C-08 | §7 결정 기록 표 10행(D-01~D-10), 채택안·기각안·다시 볼 조건 열 확인 |
| **fixed** | 87987 | C-10 | 매직링크를 §0.2 비목표에 등재 + D-07 연결. 구현자가 임의 추정할 여지 제거 |
| **fixed** | 87988 | C-11 | §0.4에 보증/비보증 분리 서술 확인 |
| **fixed** | 87989 | C-12 | §1에 "en 통일은 결정이지 다국어 지원이 아님" 명시 + D-03 + 미결 #2 |
| **fixed** | 87990·87991·87992·87993 | C-13~C-16 | 각각 §1·§5에 한계 명시 + D-04·D-07·D-08 등재. 코드 반영은 별도 결정으로 이월(합리적) |
| **fixed** | 87995 | C-19 | **코드까지 수정 확인** — `docsEnabled`→`docsAccess(sourceIp)`, 시크릿 `docs_allow_ip` 게이트, 두 진입점 반영, 유닛 테스트 갱신. §1 인증 문장도 두 엔드포인트로 축소 |
| **fixed** | 87997 | C-23 | 종료 조건보다 강한 조치 — 사무실 IP를 `develop` 테넌트 전용으로 한정하고 prod 앱 키에서 배제 |
| **fixed** | 87998 | C-24 | §2에 무중단 교체 절차(복수 키 → 전환 → 구 키 제거) 명시 |
| **fixed** | 87999 | C-25 | §1에 시도 예산 계산(10×5=50/h) + 활성 코드 없는 검증 미소비 + 스로틀 공유 명시 |
| **fixed** | 88000 | C-27 | 에러 표를 상한 429 / 스로틀 429 두 행으로 분리, 재시도 열 신설, 정시 리셋 명시 |
| **fixed** | 88001 | C-28 | §1에 클라이언트 재시도 규칙을 계약으로 명시 |
| **fixed** | 88002 | C-29 | **코드까지 구현 확인** — `issue()`에서 발송 실패 시 `codeStore.remove` best-effort, 유닛 테스트 2건 추가 |
| **fixed** | 88003 | C-30 | 4건 모두 확인 — 언어 폴백 명시, 미지 필드 무시 명시 + OpenAPI `additionalProperties` false→true, 404 행 추가, 명시 라우트 원칙(D-05) |
| ~~active~~ → **fixed(Jack)** | 87977 | 총평(C-32 흡수) | 우리 판단은 미해소(알람 구성 무변경)였으나 2026-09-02 Jack이 resolve. 잔여는 C-46 |
| **active** | 87983 | C-06 | §0.6 자리는 생겼으나 목표치 3개 전부 "미확정"으로 미결 #5 이월 (C-39) |
| **active** | 87984 | C-07 | §0.7 신설했으나 자릿수 추정도 없이 전부 미결 #6 이월 (C-40) |
| ~~active~~ → **fixed(Jack)** | 87986 | C-09 | 목표시점 6건 전부 "미정"이라 우리는 Active로 두었으나 Jack이 resolve. 잔여는 C-41 |
| ~~active~~ → **fixed(Jack)** | 87994 | C-18 | **주의** — 문서화만 되고 총량 상한은 D-02에 "미구현"으로 남아 있는데 스레드가 닫혔다. 리뷰 최우선 보안 지적 |
| ~~active~~ → **fixed(Jack)** | 87996 | C-20 | **주의** — 페퍼는 D-10에 "미구현"으로 남아 있는데 스레드가 닫혔다. 리뷰 최우선 보안 지적 |

> **2026-09-02 후속**: 우리가 Active로 남긴 6건 중 4건(87977·87986·87994·87996)을 Jack이 직접 resolve해 현재 열린 스레드는 **87983·87984 두 건**이다. 특히 87994(총량 상한)·87996(페퍼)는 Jack 자신의 결정 기록에 "미구현"으로 적힌 항목이라, 스레드가 닫히면 추적 지점이 D-02·D-10 두 줄만 남는다.
>
> Resolve 판단 기준: 지적의 **핵심 요구가 충족**되면 fixed. 코드 변경이 필요한 항목을 문서화 + 결정 기록 등재로 처리한 것은, 이미 apply된 리소스라는 사정과 다시 볼 조건이 함께 적힌 점을 보아 수용했다. 다만 **보안 갭 두 건(총량 상한·페퍼)** 은 "적어 두었다"가 해결이 아니므로 Active로 남긴다.
>
> 참고: 통상 남의 PR 스레드는 우리가 resolve하지 않으나, 이번 건은 우리가 단 리뷰 스레드이고 사용자 지시가 있어 처리했다.

---

## 신규 지적 (Round 2) — 게시 현황

| 항목 | thread | 상태 | 요지 |
| --- | --- | --- | --- |
| C-37 🔴 | 88235 | Active | 회신에만 나온 "그룹사 내부 어드민 전용" 전제가 문서에 없음 |
| C-38 🟡 | 88237 | Active | 다시 볼 조건이 목표시점 없는 미결에 걸려 발동하지 않음 |
| C-39 🟡 | 88238 | Active | 성능·가용성 목표 전건 미확정 (87983 잔여) |
| C-41 🟡 | 88239 | Active | 미결 표 목표시점 6건 전부 미정 |
| C-42 🟡 | 88240 | Active | 스펙 본문만 갱신되고 OpenAPI 정의가 뒤처짐 + 온보딩 예시에 `docs_allow_ip` 누락 |
| C-44 🟡 | 88241 | Active | 키 정기 교체 보류 근거가 §0.3 선언과 어긋남 |
| C-46 🟡 | 88243 | Active | 바운스 대응 절차·발송 실패 경보 미해소 |
| C-40 🟡 | — | **미게시** | 발송량 자릿수 추정도 없음 (87984 잔여) |
| C-43 🟢 | — | **미게시** | 발송 실패 후 삭제가 조건 없는 삭제 |
| C-45 🟢 | — | **미게시** | 회신과 미결 표의 진행 상태 불일치 |

---

## 신규 지적 (Round 2) — 상세

## C-37 · docs/otp-mail-api-spec.md:36 · 🔴 · 회신에만 나온 "그룹사 내부 어드민 전용" 전제가 문서에 없다 · [thread 88235] · **Resolved(fixed)**

- **원문**
  > `**범위**: 이메일로 6자리 코드를 발급·발송하고, 그 코드를 검증한다. 재전송을 포함한다.`
  > (총평 회신) `언어가 한국어/영어 둘뿐인 것은 아직 기획안이 확정되지 않았고 바텍 그룹사 내부 어드민에서만 사용하기에 우선 한국어/영어만 지원하도록 구현하였습니다.`
- **기준**: spec-standard.md §1.2 Product Scope · §2.5 User Classes · abc-spec-review 체크리스트 C·D
- **문제**: 이 전제는 문서 어디에도 없다. 상단은 여전히 세 제품 공용이라고만 하고, §0.3의 최종 사용자 행은 "메일 수신, 코드 입력"뿐이라 각국 일반 제품 사용자로 읽힌다.
- **근거**: 이 전제 하나가 여러 결정의 근거를 동시에 바꾼다. 사용자가 내부 어드민이면 발송량은 자릿수가 작아 예상치를 지금 낼 수 있고(C-40), 언어를 영어로 통일하는 것도 합리적이며(D-03), 총량 상한 부재의 위험도 낮아진다(D-02). 반대로 문서에 없으면 다음 사람이 일반 사용자용으로 확장하면서 그 판단들을 근거 없이 물려받는다. 지금은 리뷰 답변에만 있어서 PR이 닫히면 사라진다.
- **제안**: §0.2 범위에 "대상 사용자는 바텍 그룹사 내부 어드민"을 못 박고, 일반 최종 사용자로 확대될 때 함께 다시 볼 항목(D-02 총량 상한, D-03 언어, 미결 #6 발송량)을 그 문장에 연결한다. §0.3 최종 사용자 행에도 같은 성격을 적는다.
- **Comment** (게시본)

```text
회신 중에 언어를 한국어와 영어로만 둔 이유로 바텍 그룹사 내부 어드민에서만 사용한다는 말씀을 주셨는데, 이 전제가 문서에는 없습니다.

이게 꽤 큰 전제로 보입니다. 사용자가 내부 어드민이면 발송량은 자릿수가 작아 지금도 추정할 수 있고, 언어를 영어로 통일하는 것도 자연스럽고, 총량 상한이 없는 것의 위험도 낮아집니다. 지금 남기신 결정 여러 개가 사실 이 전제 위에 서 있는 셈입니다.

그런데 문서 상단은 세 제품 공용이라고만 하고 사용자 계층 표의 최종 사용자 행도 메일 수신과 코드 입력뿐이라, 각국 일반 제품 사용자로 읽힙니다. 이 전제가 리뷰 답변에만 있으면 이 PR이 닫히는 순간 사라지고, 다음 사람이 일반 사용자용으로 넓히면서 판단들만 그대로 가져가게 됩니다.

범위 절에 대상 사용자가 그룹사 내부 어드민이라는 점을 적어 주시고, 일반 사용자로 확대될 때 함께 다시 볼 항목으로 총량 상한과 언어 결정과 발송량 미결을 연결해 주시면 좋겠습니다.
```

- **게시** · thread 88235 · 2026-09-02 04:46 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 06:29 · cid 2]**
  > 반영했습니다. 대상 사용자가 바텍 그룹사 내부 어드민이라는 전제를 §0.2 범위 절 머리에 명시하고, §0.3 사용자 계층 표의 최종 사용자 행에도 '(그룹사 내부 어드민)'을 달았습니다. 이 전제 위에 서 있는 결정들(발송량이 자릿수로 작음, en 통일, 총량 상한 없음의 위험이 낮음)을 그 문단에서 연결했고, 대상이 일반 제품 사용자로 넓어지면 이 결정들을 전부 다시 봐야 함을 결정기록 D-11로 남겼습니다 — 확대는 기능 추가가 아니라 범위 변경임을 명시했습니다.
- 조치: §0.2 머리에 '대상 사용자 = 그룹사 내부 어드민' 전제 명시 + §0.3 최종 사용자 행 갱신 + **D-11 신설**(확대는 기능 추가가 아니라 범위 변경) · 상태: **Resolved(fixed)**

## C-38 · docs/otp-mail-api-spec.md:438 · 🟡 · 다시 볼 조건이 목표시점 없는 미결에 걸려 있어 발동하지 않는다 · [thread 88237] · **Resolved(fixed)**

- **원문**
  > `| D-02 | 2026-09-01 | 발송 상한 축 | 수신자별만 | 테넌트 총량 | **미구현·리뷰 지적.** 다시 볼 조건: 발송량 실측(#6) 또는 키 유출 시나리오 구체화 시 총량 행 추가 검토. |`
- **기준**: spec-standard.md "핵심 결정 — 대안 검토 박스"(재검토 조건) · abc-spec-review 체크리스트 J
- **문제**: D-02의 재검토 조건은 미결 #6(발송량 실측)인데, 그 미결의 목표시점이 "미정"이다. 조건이 조건에 걸려 있고 그 조건에 날짜가 없으니 아무 일도 일어나지 않는다. D-03("현지 제품팀 요구")과 D-06("발송량·경합 관측")도 관찰 주체와 시점이 없어 같은 성격이다.
- **근거**: 결정 기록의 값은 "언젠가 다시 본다"가 아니라 "무엇이 오면 다시 본다"에 있다. 그 무엇이 사람의 자발적 관찰에만 달려 있으면 기록은 남고 재검토는 안 된다. 하필 이 구조에 걸린 D-02가 리뷰에서 가장 무겁게 지적된 항목이다.
- **제안**: 조건마다 누가 언제 확인하는지를 붙인다. 관측 기반 조건은 날짜 기반 재확인으로 바꾸는 편이 실효적이다 — 예를 들어 D-02는 발송량 실측이 아니라 첫 제품 온보딩 완료 시점에 무조건 한 번 다시 보는 식이다.
- **Comment** (게시본)

```text
결정 기록 표 잘 봤습니다. 다시 볼 조건까지 적어 두신 게 좋았는데, 한 가지 구조가 걸립니다.

발송 상한 항목의 다시 볼 조건이 발송량 실측인데, 그 실측은 미결 표에 있고 목표시점이 미정입니다. 조건이 조건에 걸려 있고 그쪽에 날짜가 없어서 실제로는 발동하지 않는 구조입니다. 언어 결정과 스로틀 격리 항목도 현지 제품팀 요구나 경합 관측처럼 사람의 자발적 관찰에 달려 있어 성격이 같습니다.

결정 기록의 값은 언젠가 다시 본다가 아니라 무엇이 오면 다시 본다에 있다고 보는데, 그 무엇에 주체와 시점이 없으면 기록만 남습니다. 조건마다 누가 언제 확인하는지를 붙여 주시고, 관측에 기대는 조건은 날짜 기반으로 바꾸시는 편이 실효적일 것 같습니다. 예를 들어 발송 상한은 실측을 기다리는 대신 첫 제품 온보딩이 끝나는 시점에 무조건 한 번 다시 보는 식입니다.
```

- **게시** · thread 88237 · 2026-09-02 04:47 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 06:29 · cid 2]**
  > 반영했습니다. 결정 기록 표에 '다시 볼 조건 (주체·시점)' 열을 별도로 두고, 관측에 기대던 조건을 이벤트/날짜 기반으로 바꿨습니다. 발송 상한(D-02)은 '발송량 실측'이 아니라 '첫 제품 온보딩 직후 실측 시점'으로, 스로틀 격리(D-06)는 '경합 관측'이 아니라 '스로틀 429 알람이 실제 발생하면'으로, 언어(D-03)·채널(D-08)은 '요구서 접수 시'로 주체와 함께 적었습니다.
- 조치: 결정 기록에 **'다시 볼 조건(주체·시점)' 열 신설**. D-02→첫 온보딩 직후 실측 시점, D-06→스로틀 429 알람 발생 시, D-03·D-04·D-08→요구 접수 시, **D-10→다음 코드 반영 배치에 포함** · 상태: **Resolved(fixed)**

## C-39 · docs/otp-mail-api-spec.md:426 · 🟡 · 성능·가용성 목표가 전부 미확정으로 이월됐다 (87983 잔여) · [thread 88238] · **Resolved(fixed)**

- **원문**
  > `| 5 | 성능·가용성 목표 확정 | 제안값만(§0.6) | 인프라+앱 | 미정 | — |`
  > (§0.6, 95줄) `| 가용성 | 목표 미확정 | 로그인 상한이므로 확정 필요 |`
- **기준**: spec-standard.md 5장·§6.3.1 · spec-philosophy §3.6(비기능 요구는 수치가 없으면 합격 판정 불가)
- **문제**: 절은 생겼는데 값이 없다. §0.6이 스스로 "앱 팀은 이 값을 알아야 자기 쪽 타임아웃·실패 처리를 설계할 수 있다"고 쓰면서 그 값을 비워 두었고, 제안값(p95 2초·300밀리초)의 근거도 없다.
- **근거**: 확정에 앱 팀 합의가 필요한 것은 이해되지만, 그 사이에도 앱 팀은 타임아웃을 정해야 한다. 확정 목표와 별개로 **현재 구성이 사실상 강제하는 값**은 지금 적을 수 있다. 검증 응답의 최악값은 Lambda 타임아웃 15초이고 콜드스타트가 그 안에 포함되며, 가용성은 단일 리전·단일 함수·단일 테이블 구성이 주는 상한이다.
- **제안**: 목표 표에 "현재 구성이 보장하는 최악값" 열을 하나 더 두고 위 값을 채운다. 앱은 그걸로 설계를 시작하고, 확정 목표는 미결 #5로 계속 둔다.
- **Comment** (게시본)

```text
성능 절이 생긴 것은 좋은데 값이 전부 미확정이라, 원래 말씀드린 판정 불가 상태는 그대로 남습니다. 절 안에서 앱 팀이 이 값을 알아야 자기 쪽 타임아웃을 설계할 수 있다고 쓰시고 그 값을 비워 두신 형태라 특히 그렇습니다.

확정에 앱 팀 합의가 필요한 것은 이해합니다. 다만 합의 전에도 앱 팀은 타임아웃을 정해야 하는데, 확정 목표와 별개로 지금 구성이 사실상 강제하는 값은 지금 적을 수 있을 것 같습니다. 검증 응답의 최악값은 람다 타임아웃 15초이고 콜드스타트가 그 안에 들어가며, 가용성은 단일 리전 단일 함수 단일 테이블 구성이 주는 상한입니다.

목표 표에 현재 구성이 보장하는 최악값 열을 하나 더 두고 그것만 채워 주시면, 앱 쪽은 설계를 시작할 수 있고 확정 목표는 미결로 계속 두셔도 될 것 같습니다.
```

- **게시** · thread 88238 · 2026-09-02 04:47 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 06:29 · cid 2]**
  > 반영했습니다. 성능 표를 '확정 목표(제안·미결)'와 '현재 구성 보장 최악값(확정)' 두 열로 나누고 최악값을 채웠습니다. 검증 응답 최악은 콜드스타트 포함 Lambda 타임아웃 15초, 발급도 동일, 가용성은 단일 리전·단일 함수·단일 테이블 구성이 주는 상한(멀티리전·페일오버 없음)으로 적었습니다. 확정 목표는 미결 #5로 계속 두되, 앱 팀은 이 최악값으로 지금 타임아웃을 설계할 수 있습니다.
- 조치: §0.6 성능 표를 확정 목표 / 현재 구성 보장 최악값 두 열로 분리하고 최악값을 채움 · 상태: **Resolved(fixed)**

## C-40 · docs/otp-mail-api-spec.md:427 · 🟡 · 예상 발송량이 자릿수 추정도 없이 이월됐다 (87984 잔여) · **미게시**

- **원문**
  > `| 6 | 예상 발송량 실측 | 미측정(§0.7). 자릿수 추정 후 갱신 | 인프라 | 미정 | — |`
- **기준**: abc-spec-review 체크리스트 E-1(데이터 규모 — 하드)
- **문제**: 원 지적의 대안으로 "정확한 수치가 어려우면 자릿수 추정과 근거라도 남기라"고 했는데, 그 추정도 남기지 않고 전부 미결로 갔다.
- **근거**: 이 숫자가 없으면 C-38의 순환이 닫히지 않는다. 그리고 C-37이 사실이라면 추정은 오히려 쉽다 — 내부 어드민 계정 수에 로그인 빈도를 곱하면 자릿수는 나온다. 실측을 기다릴 이유가 크지 않아 보인다.
- **제안**: 어드민 계정 수 기준으로 월 발송 자릿수를 한 줄 추정하고 근거를 붙인다. 실측 후 갱신은 그대로 두면 된다.
- **Comment 초안**

```text
발송량이 미측정으로 정리됐는데, 앞서 정확한 수치가 어려우면 자릿수 추정과 근거만이라도 남겨 달라고 말씀드린 부분은 빠진 것 같습니다.

이 숫자가 없으면 발송 상한 결정의 다시 볼 조건이 발동하지 않는 문제가 함께 남습니다. 그리고 사용자가 그룹사 내부 어드민이라면 추정이 오히려 쉬울 것 같습니다. 어드민 계정 수에 로그인 빈도를 곱하면 자릿수는 나오고, 그 정도면 상한 값과 경보 임계를 판단하기에 충분합니다.

계정 수 기준으로 월 발송 자릿수를 한 줄만 적고 근거를 붙여 주시면 좋겠습니다. 실측 후 갱신은 그대로 두시면 됩니다.
```

- 조치: — · 상태: **미게시**

## C-41 · docs/otp-mail-api-spec.md:420 · 🟡 · 미결 표의 목표시점이 6건 전부 미정이다 (87986 잔여) · [thread 88239] · **Resolved(fixed)**

- **원문**
  > `| # | 항목 | 상태 | 담당 | 목표시점 | 티켓 |`
- **기준**: spec-standard.md "TBD 처리 규칙"(담당자·결정 시점·관련 이슈가 **모두** 있을 것)
- **문제**: 담당 열은 다 찼고 티켓도 네 건 연결됐는데 목표시점은 여섯 건 모두 "미정"이다. 기획 확정 대기라는 사정은 이해하지만, 그러면 "확정을 기다리고 있다는 사실"을 확인하는 날짜라도 있어야 한다.
- **근거**: 담당과 시점이 함께 있어야 미결이 닫힌다. 특히 #3은 서비스를 실제로 쓸 수 있느냐를 좌우하고, #5는 앱 팀의 설계 입력이며, #6은 결정 재검토의 트리거다(C-38).
- **제안**: 확정 시점을 못 박기 어려우면 **재확인 시점**을 적는다. 예를 들어 "9월 3주차에 앱 기획과 재확인"처럼 두면 미결이 조용히 잊히지 않는다.
- **Comment** (게시본)

```text
미결 표에 담당과 티켓 열이 생긴 것은 좋은데 목표시점이 여섯 건 모두 미정입니다.

기획 확정을 기다리는 사정은 이해합니다. 다만 그러면 확정을 기다리고 있다는 사실 자체를 확인하는 날짜라도 있어야 미결이 닫힐 것 같습니다. 특히 송신 주소 항목은 서비스를 실제로 쓸 수 있느냐를 좌우하고, 성능 목표는 앱 팀 설계 입력이고, 발송량은 발송 상한 재검토의 트리거라서 셋 다 방치되면 곤란한 자리입니다.

확정 시점을 못 박기 어려우시면 재확인 시점을 적어 주시면 어떨까 합니다. 예를 들어 9월 셋째 주에 앱 기획과 다시 확인하는 식으로 두면 조용히 잊히지는 않을 것 같습니다.
```

- **게시** · thread 88239 · 2026-09-02 04:47 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 06:29 · cid 2]**
  > 반영했습니다. 미결 표의 '목표시점' 열을 '재확인 시점'으로 바꾸고, 확정 시점을 못 박기 어려운 항목은 재확인 날짜를 넣었습니다. 송신 주소·성능 목표·언어는 2026-09 셋째 주 앱 기획과 재확인, 발송량은 첫 온보딩 직후 실측으로 두어 조용히 잊히지 않게 했습니다.
- 조치: 미결 표의 '목표시점' 열을 **'재확인 시점'으로 바꾸고 6건 전부 값 기입**(#1·#2·#3·#5=2026-09 셋째 주, #4=앱 레포 결정 시, #6=첫 온보딩 직후) · 상태: **Resolved(fixed)**

## C-42 · docs/otp-mail-api-spec.md:262 · 🟡 · 스펙 본문은 고쳤으나 OpenAPI 정의가 따라오지 않았다 · [thread 88240] · **Resolved(fixed)**

- **원문**
  > (§2 시크릿 예시) `  "DOC_ENABLED": false`
  > (openapi.mjs:17) `    version: "2.0.0",`
- **기준**: abc-spec-review 체크리스트 K(연계 산출물 정합성)
- **문제**: 이번 반영에서 스펙 본문만 고쳐지고 코드 안 OpenAPI 정의가 뒤처진 곳이 세 군데 있다.
  1. **검증 경로에 429가 없다.** 스펙은 이번에 "스로틀 429는 검증 호출에도 걸린다"를 명시했는데(206~207줄), OpenAPI의 `/v1/otp/verify` 응답은 400·401·403·503뿐이다.
  2. **404가 어느 경로에도 없다.** 스펙 에러 표에는 추가됐다.
  3. **버전이 어긋난다.** 문서 헤더는 v3인데 `info.version`은 여전히 2.0.0이다.
  또 §2의 시크릿 예시 JSON에 `docs_allow_ip`가 없다. §1은 그 키를 권장 방식으로 설명하고 인프라 코드 주석에도 반영돼 있는데 예시만 빠져서, 예시를 그대로 복사해 온보딩하면 플래그를 켜는 순간 전면 공개 구성이 된다.
- **근거**: 계약을 서술하는 곳이 셋이라는 문제(C-35)가 그대로 남은 상태에서 본문만 갱신되면 격차가 벌어진다. 이번 라운드가 그 첫 사례다.
- **제안**: OpenAPI의 검증 응답에 429를, 두 경로에 404를 추가하고 `info.version`을 문서와 맞춘다. §2 예시 JSON에 `docs_allow_ip`를 넣는다. 겸해서 허용목록이 없을 때 열리는 쪽이 기본인데, 안전한 기본값은 반대 방향이다.
- **Comment** (게시본)

```text
이번 반영에서 스펙 본문만 고쳐지고 코드 안 OpenAPI 정의가 따라오지 않은 곳이 몇 군데 있습니다.

검증 경로 응답에 429가 없습니다. 스펙 에러 표에는 스로틀 429가 검증 호출에도 걸린다고 새로 적으셨는데 OpenAPI의 검증 응답은 400과 401과 403과 503뿐입니다. 404도 두 경로 어디에도 없는데 스펙 표에는 추가되어 있습니다. 그리고 문서 헤더는 v3인데 OpenAPI의 버전 값은 여전히 2.0.0입니다.

별건으로, 온보딩 절의 시크릿 예시에 docs_allow_ip가 빠져 있습니다. 앞 절에서 그 키를 권장 방식으로 설명하시고 인프라 코드 주석에도 반영돼 있는데 예시만 없어서, 예시를 그대로 복사해 온보딩하면 플래그를 켜는 순간 전면 공개 구성이 됩니다. 예시에 키를 넣어 주시면 좋겠습니다. 겸해서 허용목록이 없을 때 열리는 쪽이 기본인데, 안전한 기본값은 반대 방향이 아닐까 합니다.
```

- **게시** · thread 88240 · 2026-09-02 04:48 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 06:29 · cid 2]**
  > 반영했습니다. OpenAPI를 스펙에 맞췄습니다 — 검증 응답에 404와 429(스테이지 스로틀, 검증에도 걸림)를 추가하고, 발급에도 404를 넣었으며, Error 컴포넌트 설명에 404와 두 종류 429를 반영했습니다. info.version을 2.0.0에서 3.0.0으로 올려 문서 헤더 v3와 맞췄습니다. 온보딩 절 시크릿 예시에도 docs_allow_ip를 넣어, 예시를 그대로 복사해도 플래그를 켜는 순간 전면 공개가 되지 않도록 했습니다.
- 조치: OpenAPI 정합 — 두 경로에 404, 검증에 429 추가, Error 설명 갱신, `info.version` 3.0.0. §2 시크릿 예시에 `docs_allow_ip` 추가 · 상태: **Resolved(fixed)**

## C-43 · src/core/otp-service.mjs:216 · 🟢 · 발송 실패 후 삭제가 조건 없는 삭제다 · **미게시**

- **원문**
  > `      try {`
  > `        await codeStore.remove(codeKey);`
- **기준**: abc-spec-review 체크리스트 L(동시성 경계)
- **문제**: 같은 수신자에 대해 발급이 겹칠 때, 먼저 시작한 요청의 발송이 실패하면 그사이 다른 요청이 저장한 **새 코드까지** 지운다. 사용자는 두 번째 메일을 받았는데 검증이 `not_found`가 된다.
- **근거**: 창이 좁고 재전송 시나리오라 실제 영향은 작다. 다만 소비 마킹에서 이미 조건부 쓰기를 쓰고 있으므로(해시 일치 AND 소비 마커 부재) 같은 패턴을 여기에도 적용하면 정확해지고 코드 성격도 일관된다.
- **제안**: 방금 쓴 해시가 그대로일 때만 지우는 조건부 삭제로 바꾼다. 조건 불일치는 "누군가 이미 덮어썼다"는 뜻이므로 그냥 넘어가면 된다.
- **Comment 초안**

```text
발송 실패 시 레코드를 지우는 처리 잘 봤습니다. 아주 작은 것 하나만 짚습니다.

지금 삭제가 조건 없는 삭제라, 같은 수신자에 대해 발급이 겹치면 먼저 시작한 요청의 발송이 실패했을 때 그사이 다른 요청이 저장한 새 코드까지 지웁니다. 사용자는 두 번째 메일을 받았는데 검증이 발급된 적 없음으로 나오는 형태입니다. 창이 좁고 재전송 상황이라 실제 영향은 작을 것 같습니다.

방금 쓴 해시가 그대로일 때만 지우는 조건부 삭제로 두시면 정확해집니다. 소비 마킹에서 이미 같은 방식의 조건부 쓰기를 쓰고 계셔서 코드 성격도 맞습니다. 조건이 어긋나면 누군가 이미 덮어썼다는 뜻이니 그냥 넘어가면 됩니다.
```

- 조치: — · 상태: **미게시**

## C-44 · docs/otp-mail-api-spec.md:268 · 🟡 · 정기 교체 보류 근거가 문서의 다른 선언과 어긋난다 · [thread 88241] · **Resolved(fixed)**

- **원문**
  > `- **키 로테이션(무중단)**: **같은 테넌트에 키를 복수로 둘 수 있다.** 교체는 **신규 키`
  > (회신) `키가 유출되더라도 발신자가 각 테넌트별 허용 ip가 아니면 이메일이 발송되지 않고 코드에서 막히기 때문에 보류했습니다.`
- **기준**: spec-standard.md §6.2 · abc-spec-review 체크리스트 D(섹션 간 일관성)
- **문제**: 보류 근거가 소스 IP 허용목록의 방어력에 기대는데, 같은 문서 §0.3이 "소스 IP 허용목록이 현실적으로 서버만 통과시키지만 **그건 결과이지 계약이 아니다**"라고 스스로 적었다. 또 §2에서 `develop` 테넌트는 사무실 IP를 허용하므로, 그 키는 사무실에서 나가는 누구나 쓸 수 있어 IP 방어선이 사실상 없다.
- **근거**: 무중단 절차가 생긴 것은 진전이므로 이 항목 자체는 해결로 봤다. 다만 보류 근거는 문서 안에서 서로 어긋나 있어, 남겨 두면 나중에 잘못된 전제로 읽힌다.
- **제안**: 최소한 `develop` 키에는 교체 주기를 둔다. prod 키의 보류를 유지한다면 근거를 IP 방어력이 아니라 다른 것(엔드포인트가 좁아 유출 시 피해가 고정 템플릿 발송에 그침)으로 바꾼다.
- **Comment** (게시본)

```text
무중단 교체 절차가 생긴 것으로 이 항목은 정리됐다고 봤습니다. 다만 정기 교체를 보류하신 근거만 한 가지 말씀드립니다.

보류 사유가 키가 유출돼도 허용 IP가 아니면 막힌다는 것인데, 같은 문서 책임 경계 절에서 소스 IP 허용목록이 현실적으로 서버만 통과시키지만 그건 결과이지 계약이 아니라고 적어 두셨습니다. 그리고 온보딩 절에서 develop 테넌트는 사무실 IP를 허용하므로, 그 키는 사무실에서 나가는 누구나 쓸 수 있어 IP 방어선이 사실상 없습니다.

최소한 develop 키에는 교체 주기를 두시는 편이 좋겠습니다. prod 키의 보류를 유지하신다면 근거를 IP 방어력이 아니라 엔드포인트가 좁아 유출 시 가능한 일이 고정 템플릿 발송에 그친다는 쪽으로 바꾸시면 문서 안에서 서로 어긋나지 않습니다.
```

- **게시** · thread 88241 · 2026-09-02 04:48 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 06:29 · cid 2]**
  > 반영했습니다. 문서 안 자기모순을 해소했습니다. develop 키는 사무실 IP를 허용해 IP가 방어선이 못 되므로 정기 교체(권장 분기)를 두고, prod 앱 키의 정기 교체 보류 근거는 IP 방어력이 아니라 '엔드포인트가 좁아 유출 시 가능한 일이 고정 템플릿 발송에 그친다'는 쪽으로 바꿨습니다. 유출 정황이 있으면 무중단 순서로 즉시 교체하는 것은 그대로입니다.
- 조치: develop 키는 정기 교체(권장 분기) 신설, prod 키 보류 근거를 IP 방어력에서 '엔드포인트가 좁아 피해가 고정 템플릿 발송에 그침'으로 교체 · 상태: **Resolved(fixed)**

## C-45 · docs/otp-mail-api-spec.md:424 · 🟢 · 회신과 미결 표의 진행 상태가 어긋난다 · **미게시**

- **원문**
  > `| 3 | prod 앱별 고정 egress IP 확보(사무실 IP는 develop 키에만) | 수집 중 (특히 Dent VN) | 각 제품팀 | 미정 | AB#5635 |`
  > (회신) `앱별 송신 주소는 각 애플리케이션들의 VM public ip 및 NAT Gateway IP를 허용하도록 지정하였고`
- **문제**: 회신은 지정을 마쳤다고 하는데 미결 표는 여전히 수집 중이다. 완료인지 진행 중인지 문서로 판단할 수 없다.
- **제안**: 어느 쪽인지 상태를 맞춘다. 일부만 확보됐다면 어느 제품이 남았는지 적는다.
- **Comment 초안**

```text
송신 주소 건은 회신에서 각 앱의 서버 공인 주소와 NAT 게이트웨이 주소를 허용하도록 지정하셨다고 하셨는데, 미결 표에는 여전히 수집 중으로 남아 있습니다. 완료인지 진행 중인지 문서만으로는 판단이 어렵습니다. 일부만 확보된 상태라면 어느 제품이 남았는지까지 적어 주시면 좋겠습니다.
```

- 조치: — · 상태: **미게시**

## C-46 · docs/otp-mail-api-spec.md:312 · 🟡 · 바운스 대응 절차와 발송 실패 경보가 그대로 남았다 (총평 잔여) · [thread 88243] · **Resolved(fixed)**

- **원문**
  > `- 장애 신호 3종(\`secret refresh FAILED\` / \`rate limiter unavailable\` / \`code store`
- **문제**: 알람 구성(`monitoring.tf`)은 이번 라운드에서 변경되지 않았다. 총평에 적었던 두 가지가 그대로다. 바운스 알람이 지시하는 대응(함수 로그에서 수신자 찾기)은 로그에 바운스가 없어 수행할 수 없고, 502에는 경보가 없다. 이번에 502 경로에 삭제 실패 로그가 하나 더 늘었는데 그것도 경보 대상이 아니다.
- **제안**: 발송 이벤트 대상을 구독하면 첫째가 풀리고 총량 감시도 함께 얹을 수 있다. 최소한 발송 실패와 삭제 실패 로그를 경보 대상에 추가한다.
- **Comment** (게시본)

```text
총평에 적었던 알람 관련 두 가지는 이번 반영에 포함되지 않은 것 같아 다시 남깁니다.

바운스 알람 설명이 지시하는 대응이 함수 로그에서 수신자와 테넌트를 찾아 억제하라는 것인데, 함수 로그에는 발송만 있고 바운스는 없어서 그대로는 수행이 어렵습니다. 그리고 발송 실패에는 여전히 경보가 없습니다. 이번에 502 경로에 삭제 실패 로그가 하나 더 늘었는데 그것도 경보 대상이 아닙니다.

발송 이벤트 대상을 구독하시면 첫 번째가 풀리고 총량 감시도 같이 얹을 수 있습니다. 당장은 발송 실패와 삭제 실패 로그만 경보 대상에 추가하셔도 될 것 같습니다.
```

- **게시** · thread 88243 · 2026-09-02 04:49 · 전규현 명의
- **[임건혁(Jack) · 2026-09-02 06:29 · cid 2]**
  > 일부 반영하고 일부는 후속으로 남겼습니다. 발송 실패(502)와 D-09 삭제 실패 로그를 알람 대상에 추가했습니다(monitoring.tf의 send-failed / code-cleanup-failed). 바운스 대응 지시가 함수 로그로는 수행 불가라는 지적은 맞습니다 — 함수 로그엔 발송만 있어 바운스는 관측되지 않습니다. 이를 실행 가능하게 하려면 SES 아이덴티티의 바운스/컴플레인 알림(또는 SESv2 configuration set 이벤트 대상)을 SNS로 구독해 수신자·테넌트를 기록해야 하는데, 이는 이 알람 번들이 아니라 SES 쪽 신규 배선이라 후속으로 뒀습니다. 그 이벤트 스트림이 테넌트 총량 감시(D-02)도 함께 가능하게 하므로 두 작업을 묶어 처리할 계획이며, monitoring.tf에 그 근거를 주석으로 남겼습니다.
- 조치: **알람 추가** — `send-failed`·`code-cleanup-failed`(monitoring.tf). 바운스 가시성 갭은 SES 이벤트 구독이 필요한 신규 배선이라 후속으로 두고 근거를 코드 주석에 명시(D-02 총량 감시와 묶어 처리 예정) · 상태: **Resolved(fixed)**

---

## Round 1 미게시분 재확인

| 항목 | 반영본에서의 상태 |
| --- | --- |
| C-17 저장 리전·규제 검토 | **여전히 없음.** §0.5 종속성 표에도 개인정보 국외 이전 관점은 없다. C-37(내부 어드민 전용)이 사실이면 위험이 낮아지므로 그 전제와 함께 한 줄이면 닫힌다 |
| C-21 통과 허용 정책 근거 | **그대로.** §5 장애 정책 표 문구 무변경 — 카운터와 코드 스토어가 같은 테이블이라 근거가 성립하지 않는 점 미해소 |
| C-22 소스 IP 획득 경로 | **그대로.** EKS 이관 계약 표에 소스 IP 결정 방식 행 없음. `/docs` 게이트가 소스 IP에 의존하게 되면서 오히려 중요해졌다 |
| C-26 열거 방지 속성 명시 | 부분 — §0.4가 보증 범위를 적었으나 발급이 주소 존재를 노출하지 않는다는 장점은 여전히 미기술 |
| C-31 엣지 3건 | 부분 — 502 경로는 해소(D-09). 사유 관측 가능성과 동시 발급 결과는 그대로 |
| C-33 로그 수신자 원문 | **그대로.** §4 로그 예시 무변경 |
| C-34 요청 식별자·보존 근거 | **그대로.** 로그에 요청 식별자 없음, 30일 근거 없음 |
| C-35 계약 정본·버전 정책 | **악화.** 문서는 v3로 올렸으나 OpenAPI는 2.0.0 그대로 → C-42 |
| C-36 문서 내 불일치 | **해소.** 기준 주소가 미결 표에서 빠졌고, 환경변수 선언 불일치는 §5에 명시됨 |

---

# Round 3 — Jack 2차 반영본 재검토 (2026-09-03)

> **대상**: 헤드 `4f0d868`(67dec10 이후 2커밋 — `b6369a6` 2차 리뷰 반영 / `4f0d868` 매직링크 미고려 확정). 스펙 80줄 변경 · `openapi.mjs` 12줄 · `monitoring.tf` 28줄 추가.
> **Jack 회신**: 열린 7개 신규 스레드 전건 회신(2026-09-02 06:29) + 매직링크 스레드(87987)에 개발팀장 확정 후속(2026-09-03 02:10). `88243`은 Jack이 직접 close.
> **처리**: 문서·코드 실물 대조 후 **7건 Resolve**, **1건 Active 유지**. 현재 열린 스레드는 `87984` 하나.

## 스레드 처리 결과

| 상태 | thread | 항목 | 확인 내용 |
| --- | --- | --- | --- |
| **fixed** | 87983 | C-06 | §0.6에 "현재 구성 보장 최악값" 열 신설 — 발급·검증 = 콜드스타트 포함 Lambda 15초 상한, 가용성 = 단일 리전·함수·테이블 상한(멀티리전·페일오버 없음). 앱 팀이 지금 타임아웃을 설계할 수 있게 됨 |
| **fixed** | 88235 | C-37 | §0.2 머리에 대상 사용자 전제 명시 + §0.3 최종 사용자 행 갱신 + **D-11 신설**. "확대는 기능 추가가 아니라 범위 변경"까지 문장으로 박음 |
| **fixed** | 88237 | C-38 | 결정 기록에 "다시 볼 조건(주체·시점)" 열 신설. 관측 의존 조건이 전부 이벤트·날짜 기반으로 바뀜 — D-06은 "스로틀 429 알람이 실제 발생하면"으로, **D-10은 "다음 코드 반영 배치에 포함(보안·저비용이라 대기 불필요)"으로 강화** |
| **fixed** | 88238 | C-39 | 성능 표를 확정 목표 / 보장 최악값 두 열로 분리하고 최악값을 채움 |
| **fixed** | 88239 | C-41 | "목표시점" → "재확인 시점"으로 열을 바꾸고 6건 전부 값 기입 |
| **fixed** | 88240 | C-42 | OpenAPI에 404(두 경로)·429(검증) 추가, Error 설명 갱신, `info.version` 3.0.0. §2 예시에 `docs_allow_ip` 추가 |
| **fixed** | 88241 | C-44 | develop 키 정기 교체 신설, prod 키 보류 근거를 IP 방어력에서 엔드포인트 협소성으로 교체 — §0.3과의 자기모순 해소 |
| **fixed(Jack)** | 88243 | C-46 | `send-failed`·`code-cleanup-failed` 알람 추가 확인. 바운스 가시성 갭은 SES 이벤트 구독이 필요한 신규 배선이라 후속으로 두고 `monitoring.tf`에 근거 주석을 남김 — D-02 총량 감시와 묶어 처리 예정 |
| **fixed** | 87987 | C-10 | 개발팀장 확정으로 매직링크 미고려가 결정됨. 비목표 표 문구를 "코드 입력 방식으로 확정, 자동 인증 URL 미고려"로 바꾸고 D-07의 남는 사유를 purpose 축으로 축소 |
| **fixed** | 87984 | C-07 | §0.7 무변경(자릿수 추정 없음). 재확인 시점만 기입 — 리뷰 종료로 close, 잔여는 C-40·C-47로 미게시 보관 |

> **평가**: 이번 라운드는 지적을 문서에 옮기는 수준을 넘어 **근거의 정합**까지 손봤다. 특히 D-10(페퍼)의 재검토 조건을 "다음 코드 반영 배치에 포함"으로 못 박은 것과, 키 교체 보류 근거를 §0.3과 어긋나지 않게 바꾼 것은 요청 이상이다. 바운스 가시성 갭도 "못 한다"가 아니라 **무엇을 해야 실행 가능해지는지를 코드 주석에 남기고 D-02와 묶은 것**이라 추적이 끊기지 않는다.
>
> 남은 실질 미구현은 **발송 총량 상한(D-02)** 과 **코드 저장 페퍼(D-10)** 두 건이며, 둘 다 재검토 시점이 명시됐다. 페퍼는 "다음 코드 반영 배치", 총량은 "첫 온보딩 직후 실측 시점 + SES 이벤트 구독 작업과 묶음"이다.

---

## 신규 지적 (Round 3)

## C-47 · docs/otp-mail-api-spec.md:38 · 🟢 · §0.2가 §0.7을 가리키는데 §0.7에는 추정치가 없다 · **미게시**

- **원문**
  > (§0.2, 신설분) `이 전제 위에 여러 결정이 서 있다 — 발송량이 자릿수로 작아 지금도 추정 가능하고(§0.7), …`
  > (§0.7, 무변경) `세 제품이 다섯 리전에서 붙은 뒤의 예상 발송량은 **아직 미측정**(미결 #6).`
- **기준**: abc-spec-review 체크리스트 D(섹션 간 일관성)
- **문제**: 이번에 추가된 §0.2 문장은 "발송량이 자릿수로 작아 **지금도 추정 가능**하다"며 §0.7을 가리키는데, §0.7은 손대지 않아 여전히 미측정이다. 가리킨 곳에 추정치가 없다.
- **근거**: 사소한 참조 불일치지만, 하필 그 추정치가 D-02(총량 상한) 재검토의 입력이라 비어 있는 것이 눈에 띈다. 그리고 §0.2가 "지금도 추정 가능"이라고 단언한 이상 §0.7에 한 줄 쓰는 데 드는 비용이 거의 없다는 뜻이기도 하다.
- **제안**: §0.7에 내부 어드민 계정 수 × 로그인 빈도 기준의 자릿수 추정을 한 줄 넣는다. 실측 후 갱신은 그대로 두면 된다. 그것이 어려우면 §0.2의 "지금도 추정 가능하고"를 "실측 시 자릿수가 작을 것으로 보이고"로 완화한다.
- **Comment 초안**

```text
이번에 넣으신 대상 사용자 전제 문단이 발송량이 자릿수로 작아 지금도 추정 가능하다면서 예상 발송량 절을 가리키는데, 그 절은 그대로라 여전히 미측정입니다. 가리킨 곳에 추정치가 없는 상태입니다.

사소한 참조 문제지만, 하필 그 숫자가 총량 상한 재검토의 입력이라 비어 있는 게 눈에 띕니다. 그리고 지금도 추정 가능하다고 쓰셨으니 한 줄 적는 비용은 거의 없다는 뜻이기도 할 것 같습니다.

어드민 계정 수와 로그인 빈도 기준으로 자릿수만 한 줄 넣어 주시면 좋겠습니다. 실측 후 갱신은 그대로 두시면 됩니다. 그게 어려우시면 반대로 앞 문단의 지금도 추정 가능하고를 실측하면 자릿수가 작을 것으로 보이고 정도로 완화하셔도 됩니다.
```

- 조치: — · 상태: **미게시**
