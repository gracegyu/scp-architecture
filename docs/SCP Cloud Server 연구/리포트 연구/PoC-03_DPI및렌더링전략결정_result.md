# PoC-03 결과 보고서: DPI 및 렌더링 전략 결정

## 1. 개요

### 1.1 검증 목표

화면 표시, 프린트, 폰트 처리 시 발생하는 72 DPI, 96 DPI, 300 DPI 등의 복잡한 관계를 정리하고, px, pt 단위 사용 및 PDF 프린트 방안을 포함한 최적의 하나의 통합 렌더링 전략을 결정합니다. PoC-02에서 결정한 mm 좌표계와 완벽하게 통합되도록 폰트 크기, 좌표 변환, 인쇄 품질을 일관되게 처리하는 방안을 수립합니다.

### 1.2 평가 기준 및 가중치

| 항목 | 가중치 | 평가 기준 |
|------|--------|-----------|
| **개발 복잡도** | 35% | 구현 난이도, 코드 복잡도, 유지보수 비용 |
| **인쇄 정확도** | 30% | 인쇄 시 실제 물리적 크기 일치, mm 좌표계 통합 |
| **일관성** | 20% | 화면과 프린트에서 동일한 물리적 크기 유지 |
| **표준 준수** | 10% | 웹 표준 및 의료 인쇄 표준 준수 |
| **성능** | 5% | 렌더링 성능 영향 최소화 |

**핵심 원칙**: 개발 비용 최소화 + 인쇄 정확도 보장

**참고**: 본 PoC는 전략 결정에 집중하며, 실제 구현 및 테스트는 PoC-10, PoC-14에서 수행합니다.

---

## 2. 최종 선정 결과

### 2.1 통합 렌더링 전략 요약

**핵심 전략**: **간단하고 실용적인 접근법**

1. **좌표 단위**: mm (PoC-02에서 결정) ✅
2. **폰트 단위**: **pt (Point)** ✅
3. **화면 렌더링 DPI**: **96 DPI 고정** ✅
4. **프린트 렌더링**: **CSS pt 단위 사용 (브라우저 자동 변환)** ✅
5. **미디어 쿼리**: **@media screen과 @media print 분리** ✅
6. **PDF 생성**: **선택적 (브라우저 프린트 기본, 고품질 필요 시 PDF)** ✅

### 2.2 선정 근거 (요약)

**개발 복잡도 최소화 (35% 가중치)**:
- CSS pt 단위 직접 사용으로 브라우저가 자동으로 DPI 변환 처리
- 추가 JavaScript 변환 로직 불필요
- 미디어 쿼리만으로 화면/프린트 분리 가능
- 개발 시간 및 유지보수 비용 최소화

**인쇄 정확도 보장 (30% 가중치)**:
- pt 단위는 물리적 크기가 고정되어 인쇄 시 정확한 크기 보장
- mm 좌표계와 pt 폰트의 수학적 관계: 1 pt = 25.4/72 mm ≈ 0.3528 mm
- 브라우저가 프린터 DPI에 맞춰 자동 변환하여 정확도 유지

**일관성 보장 (20% 가중치)**:
- 화면과 프린트에서 동일한 물리적 크기 유지
- mm 좌표계와 pt 폰트 크기의 완벽한 통합

**표준 준수 (10% 가중치)**:
- 웹 표준 (96 DPI 화면, CSS pt 단위) 준수
- 의료 인쇄 표준 (물리적 크기 보장) 준수

---

## 3. 최종 전략 상세

### 3.1 Item 1: 화면 렌더링 DPI 전략

**선정**: **96 DPI 고정 (웹 표준)**

**선정 근거**:
- 간단하고 예측 가능한 동작
- 모든 브라우저에서 일관된 동작
- 추가 DPI 감지 로직 불필요 (개발 복잡도 최소화)
- 고해상도 디스플레이에서 작게 보일 수 있으나, 의료 리포트는 정확도가 우선

**구현 방법**:
```css
/* 화면 렌더링: 96 DPI 기준 */
@media screen {
  .report-container {
    /* mm 좌표를 96 DPI 기준 픽셀로 변환 */
    /* 1 mm = 3.78 px (96 DPI 기준) */
  }
}
```

**제외된 옵션**: 동적 DPI 감지
- 이유: 복잡도 증가, 브라우저별 차이 가능성, 개발 비용 증가

---

### 3.2 Item 2: 프린트 렌더링 DPI 전략

**선정**: **CSS pt 단위 사용 (브라우저 자동 변환)**

**선정 근거**:
- 브라우저가 자동으로 프린터 DPI에 맞춰 변환 (개발 복잡도 최소화)
- 추가 JavaScript 변환 로직 불필요
- 인쇄 시 정확한 물리적 크기 보장
- 웹 표준 준수

**구현 방법**:
```css
/* 프린트 렌더링: CSS pt 단위 사용 */
@media print {
  .text-element {
    font-size: 12pt;  /* 브라우저가 프린터 DPI에 맞춰 자동 변환 */
  }
  
  .report-container {
    /* mm 좌표는 그대로 사용, 브라우저가 자동 변환 */
    width: 210mm;  /* A4 가로 */
    height: 297mm; /* A4 세로 */
  }
}
```

**제외된 옵션**:
- `@media print` + 300 DPI 가정 (수동 변환): 복잡도 증가, 실제 프린터 DPI와 다를 수 있음
- 사용자 설정 옵션: 사용자 부담 증가, 복잡도 증가

---

### 3.3 Item 3: 폰트 단위 선택

**선정**: **pt (Point) 단위 사용**

**선정 근거**:
- PoC-02 Q&A 섹션에서 권장된 단위
- 브라우저가 자동으로 DPI에 맞춰 변환 (화면 96 DPI, 프린트 프린터 DPI)
- mm 좌표계와 물리적 크기 일치 가능 (1 pt = 25.4/72 mm ≈ 0.3528 mm)
- 웹 표준 및 의료 인쇄 표준 준수
- 개발 복잡도 최소화 (CSS `font-size: 12pt` 직접 사용)
- **폰트 크기의 표준 단위로 널리 사용됨** (일반적이고 검증된 관행)

**mm 좌표계와의 통합**:
- 수학적 관계: 1 pt = 25.4/72 mm ≈ 0.3528 mm
- 화면 렌더링: 12pt → 16px (96 DPI) → 4.233mm ✅
- 프린트 렌더링: 12pt → 4.233mm (72 DPI 기준) ✅
- **결론**: Point와 mm 좌표계는 수학적으로 완벽하게 일치

**구현 예시**:
```typescript
interface TextElement {
  position: { x: 26.25, y: 74.25, unit: 'mm' },
  style: {
    fontSize: 12,  // pt 단위
    fontFamily: 'Arial'
  },
  content: '<p>의료 리포트 텍스트</p>'
}
```

```css
.text-element {
  position: absolute;
  left: 26.25mm;  /* mm 좌표 */
  top: 74.25mm;   /* mm 좌표 */
  font-size: 12pt; /* pt 폰트 크기 */
}
```

**제외된 옵션**:

1. **mm 단위**:
   - 기술적으로는 가능하나 **폰트 크기 단위로 일반적이지 않음**
   - CSS 표준에서 폰트 크기는 pt, px, em, rem 등이 권장됨
   - 브라우저 호환성 및 동작 방식이 pt 대비 덜 검증됨
   - 개발자 및 디자이너에게 혼란 가능 (폰트 크기를 mm로 지정하는 것은 비표준)
   - 좌표 단위와 통일성을 위해 고려했으나, 표준 관행을 따르는 것이 더 중요

2. **px 단위**:
   - DPI별로 다른 값 계산 필요, 복잡도 증가
   - 인쇄 시 물리적 크기 보장 불가

3. **하이브리드 (pt + px)**:
   - 복잡도 증가, 두 가지 단위 관리 필요

---

### 3.4 Item 4: 미디어 쿼리 전략

**선정**: **@media screen과 @media print 분리**

**선정 근거**:
- 화면과 프린트를 명확히 분리하여 각각 최적화 가능
- CSS만으로 처리 가능 (JavaScript 불필요)
- 개발 복잡도 최소화
- 유지보수 용이

**구현 방법**:
```css
/* 화면 렌더링 */
@media screen {
  .report-container {
    /* 96 DPI 기준 렌더링 */
    width: 210mm;
    height: 297mm;
  }
  
  .text-element {
    font-size: 12pt;  /* 브라우저가 96 DPI 기준으로 변환 */
  }
}

/* 프린트 렌더링 */
@media print {
  .report-container {
    /* mm 단위 그대로 사용, 브라우저가 프린터 DPI에 맞춰 변환 */
    width: 210mm;
    height: 297mm;
  }
  
  .text-element {
    font-size: 12pt;  /* 브라우저가 프린터 DPI에 맞춰 변환 */
  }
}
```

**제외된 옵션**: 단일 스타일 + JavaScript 동적 변환
- 이유: JavaScript 복잡도 증가, 개발 비용 증가

---

### 3.5 Item 5: PDF 생성 전략

**선정**: **선택적 (브라우저 프린트 기본, 고품질 필요 시 PDF)**

**선정 근거**:
- 개발 복잡도 최소화 (기본 기능은 브라우저 프린트 사용)
- 대부분의 사용자는 브라우저 프린트로 충분
- 고품질이 필요한 경우에만 PDF 생성 제공
- 서버 인프라 비용 절감

**구현 방법**:
1. **기본 인쇄**: 브라우저 프린트 사용
   ```typescript
   function printReport() {
     window.print();  // 브라우저 프린트 다이얼로그
   }
   ```

2. **고품질 PDF 생성** (선택적, 향후 구현):
   - 클라이언트 사이드: jsPDF 또는 React-PDF (PoC-10에서 평가)
   - 서버 사이드: Puppeteer 또는 PDFKit (PoC-10에서 평가)
   - 사용자가 "고품질 PDF 다운로드" 옵션 선택 시에만 제공

**제외된 옵션**:
- PDF 생성 필수: 개발 복잡도 증가, 서버 인프라 비용 증가
- 하이브리드 방식: 두 가지 방식 유지 필요, 복잡도 증가

**참고**: PDF 생성 라이브러리 평가 및 선택은 PoC-10에서 수행합니다.

---

### 3.6 Item 6: mm 좌표계 통합 전략

**선정**: **pt와 mm의 수학적 관계 활용**

**통합 원리**:
- mm 좌표계: 물리적 크기 보장
- pt 폰트 단위: 물리적 크기 보장
- 수학적 관계: 1 pt = 25.4/72 mm ≈ 0.3528 mm

**검증 결과** (이론적 분석):

| Point | mm (이론) | 화면 Pixel (96 DPI) | 화면 mm (96 DPI) | 인쇄 mm (72 DPI) | 일치 여부 |
|-------|----------|-------------------|-----------------|-----------------|----------|
| 12pt  | 4.233mm  | 16px              | 4.233mm         | 4.233mm         | ✅ 완벽 일치 |
| 10pt  | 3.528mm  | 13.33px           | 3.528mm         | 3.528mm         | ✅ 완벽 일치 |
| 14pt  | 4.939mm  | 18.67px           | 4.939mm         | 4.939mm         | ✅ 완벽 일치 |

**결론**: Point와 mm 좌표계는 수학적으로 완벽하게 일치하므로, 별도의 변환 로직 없이 CSS에서 직접 사용 가능합니다.

---

## 4. 통합 전략 요약

### 4.1 핵심 원칙

1. **간단함**: 브라우저 표준 기능 활용, 최소한의 코드로 구현
2. **정확도**: 인쇄 시 실제 물리적 크기 보장
3. **일관성**: 화면과 프린트에서 동일한 물리적 크기 유지
4. **표준 준수**: 웹 표준 및 의료 인쇄 표준 준수

### 4.2 구현 가이드라인

**좌표 및 폰트 크기 지정**:
```typescript
interface Element {
  position: { x: number, y: number, unit: 'mm' },
  size: { width: number, height: number, unit: 'mm' },
  style: {
    fontSize: number,  // pt 단위
    fontFamily: string
  }
}
```

**CSS 렌더링**:
```css
/* 화면 및 프린트 공통 */
.element {
  position: absolute;
  left: 26.25mm;   /* mm 좌표 */
  top: 74.25mm;     /* mm 좌표 */
  width: 50mm;      /* mm 크기 */
  height: 20mm;     /* mm 크기 */
  font-size: 12pt;  /* pt 폰트 크기 */
}

/* 화면 렌더링 (96 DPI) */
@media screen {
  .element {
    /* 추가 스타일 필요 시 */
  }
}

/* 프린트 렌더링 (프린터 DPI) */
@media print {
  .element {
    /* 추가 스타일 필요 시 */
  }
}
```

**JavaScript 변환 함수** (참고용, 실제로는 CSS에서 자동 처리):
```typescript
/**
 * mm를 픽셀로 변환 (화면 렌더링용, 96 DPI 기준)
 */
function mmToPx(mm: number, dpi: number = 96): number {
  return (mm * dpi) / 25.4
}

/**
 * pt를 픽셀로 변환 (참고용, 실제로는 CSS에서 자동 처리)
 */
function ptToPx(pt: number, dpi: number): number {
  return (pt * dpi) / 72
}
```

**실제 구현 시 주의사항**:
- CSS에서 mm와 pt 단위를 직접 사용하면 브라우저가 자동으로 변환
- JavaScript 변환 함수는 필요 시에만 사용 (대부분 CSS로 처리 가능)
- 미디어 쿼리로 화면과 프린트를 분리하여 각각 최적화

---

## 5. 옵션 비교 분석

### 5.1 화면 렌더링 DPI 전략 비교

| 옵션 | 개발 복잡도 | 일관성 | 성능 | 선정 여부 |
|------|------------|--------|------|----------|
| 96 DPI 고정 | ✅ 낮음 | ✅ 높음 | ✅ 우수 | ✅ **선정** |
| 동적 DPI 감지 | ❌ 높음 | ⚠️ 중간 | ✅ 우수 | ❌ 제외 |

**선정 근거**: 개발 복잡도 최소화, 모든 브라우저에서 일관된 동작

---

### 5.2 프린트 렌더링 DPI 전략 비교

| 옵션 | 개발 복잡도 | 인쇄 정확도 | 일관성 | 선정 여부 |
|------|------------|------------|--------|----------|
| CSS pt 단위 | ✅ 낮음 | ✅ 높음 | ✅ 높음 | ✅ **선정** |
| @media print + 300 DPI 가정 | ⚠️ 중간 | ⚠️ 중간 | ⚠️ 중간 | ❌ 제외 |
| 사용자 설정 | ❌ 높음 | ✅ 높음 | ⚠️ 중간 | ❌ 제외 |

**선정 근거**: 브라우저 자동 변환으로 개발 복잡도 최소화, 인쇄 정확도 보장

---

### 5.3 폰트 단위 선택 비교

| 옵션 | 개발 복잡도 | 인쇄 정확도 | mm 통합 | 표준 준수 | 선정 여부 |
|------|------------|------------|---------|----------|----------|
| pt (Point) | ✅ 낮음 | ✅ 높음 | ✅ 완벽 | ✅ 높음 | ✅ **선정** |
| mm (Millimeter) | ✅ 낮음 | ✅ 높음 | ✅ 완벽 | ❌ 낮음 | ❌ 제외 |
| px (Pixel) | ❌ 높음 | ❌ 낮음 | ⚠️ 복잡 | ✅ 높음 | ❌ 제외 |
| 하이브리드 | ❌ 높음 | ✅ 높음 | ⚠️ 복잡 | ⚠️ 중간 | ❌ 제외 |

**선정 근거**: 
- 개발 복잡도 최소화
- mm 좌표계와 완벽한 통합
- 인쇄 정확도 보장
- **표준 관행 준수** (폰트 크기는 pt 단위가 일반적이고 검증됨)

**mm 단위 제외 근거**:
- 기술적으로는 가능하나, 폰트 크기 단위로 일반적이지 않음
- CSS 표준에서 폰트 크기는 pt, px, em, rem 등이 권장됨
- 브라우저 호환성 및 동작 방식이 pt 대비 덜 검증됨
- 개발자 및 디자이너에게 혼란 가능 (비표준 관행)

---

### 5.4 미디어 쿼리 전략 비교

| 옵션 | 개발 복잡도 | 유지보수성 | 성능 | 선정 여부 |
|------|------------|------------|------|----------|
| @media 분리 | ✅ 낮음 | ✅ 높음 | ✅ 우수 | ✅ **선정** |
| 단일 스타일 + JS | ❌ 높음 | ⚠️ 중간 | ⚠️ 중간 | ❌ 제외 |

**선정 근거**: CSS만으로 처리 가능, 개발 복잡도 최소화

---

### 5.5 PDF 생성 전략 비교

| 옵션 | 개발 복잡도 | 인프라 비용 | 사용자 경험 | 선정 여부 |
|------|------------|------------|------------|----------|
| 선택적 (브라우저 기본) | ✅ 낮음 | ✅ 낮음 | ✅ 우수 | ✅ **선정** |
| PDF 필수 | ❌ 높음 | ❌ 높음 | ⚠️ 중간 | ❌ 제외 |
| 하이브리드 | ❌ 높음 | ⚠️ 중간 | ✅ 우수 | ❌ 제외 |
| PDF 없음 | ✅ 낮음 | ✅ 낮음 | ⚠️ 중간 | ❌ 제외 |

**선정 근거**: 개발 복잡도 최소화, 대부분의 사용자는 브라우저 프린트로 충분, 고품질 필요 시에만 PDF 제공

---

## 6. 예상 성능 및 품질 영향 분석

### 6.1 개발 복잡도

**예상 개발 시간**:
- 기본 구현: 1-2일 (CSS 미디어 쿼리 설정)
- PDF 생성 기능 (선택적): 추가 3-5일 (PoC-10에서 평가 후 결정)

**코드 복잡도**:
- CSS 기반으로 대부분 처리 가능
- JavaScript 변환 로직 최소화
- 유지보수 비용 최소화

### 6.2 인쇄 정확도

**예상 정확도**:
- mm 좌표계: ±0.1mm 이내 (의료 측정 요구사항 충족)
- pt 폰트 크기: 물리적 크기 고정 (인쇄 시 정확도 보장)
- 브라우저 자동 변환으로 프린터 DPI에 맞춰 정확한 크기 유지

**검증 필요 사항** (PoC-10에서 수행):
- 실제 프린터에서 인쇄 품질 검증
- mm 좌표계와 폰트 크기 일치 실측 검증

### 6.3 일관성

**화면과 프린트 일관성**:
- 동일한 mm 좌표계 사용
- 동일한 pt 폰트 단위 사용
- 브라우저가 각 환경에 맞춰 자동 변환하여 물리적 크기 일치

**검증 필요 사항** (PoC-14에서 수행):
- 화면과 프린트에서 동일한 물리적 크기 유지 검증
- 다양한 브라우저에서 일관성 검증

---

## 7. 결론 및 권장사항

### 7.1 최종 전략 요약

**핵심 전략**: 간단하고 실용적인 접근법

1. **좌표 단위**: mm (PoC-02에서 결정)
2. **폰트 단위**: pt (Point)
3. **화면 렌더링**: 96 DPI 고정
4. **프린트 렌더링**: CSS pt 단위 사용 (브라우저 자동 변환)
5. **미디어 쿼리**: @media screen과 @media print 분리
6. **PDF 생성**: 선택적 (브라우저 프린트 기본, 고품질 필요 시 PDF)

### 7.2 핵심 장점

1. **개발 복잡도 최소화**: CSS만으로 대부분 처리 가능, JavaScript 변환 로직 최소화
2. **인쇄 정확도 보장**: pt 단위와 mm 좌표계의 수학적 일치로 정확한 물리적 크기 보장
3. **일관성 보장**: 화면과 프린트에서 동일한 물리적 크기 유지
4. **표준 준수**: 웹 표준 및 의료 인쇄 표준 준수
5. **유지보수성**: 간단한 구조로 유지보수 비용 최소화

### 7.3 다음 단계

1. **PoC-10 (인쇄 및 Export 품질 검증)**: 
   - 실제 인쇄 품질 테스트
   - PDF 생성 라이브러리 평가 및 선택
   - mm 좌표계와 폰트 크기 일치 실측 검증

2. **PoC-14 (Element 렌더링 엔진 구현)**:
   - 결정된 전략을 실제 렌더링 엔진에 적용
   - 화면 렌더링 테스트
   - 통합 테스트 (화면과 프린트 일관성)

### 7.4 주의사항

1. **브라우저별 차이**: CSS pt 단위 처리 방식이 브라우저마다 약간 다를 수 있으므로, PoC-10에서 실제 테스트 필요
2. **프린터 DPI**: 브라우저가 프린터 DPI를 자동으로 감지하지만, 정확도는 프린터 설정에 의존
3. **PDF 생성**: 고품질이 필요한 경우 PDF 생성 기능을 추가로 제공 (PoC-10에서 평가)

---

## 8. 참고 자료

- PoC-02 결과 보고서: mm 좌표계 결정 및 Point, Pixel, mm의 관계 설명
- CSS 표준: CSS Values and Units Module Level 3 (W3C)
- 웹 표준: 96 DPI 화면 표준 (CSS Pixel)
- 의료 인쇄 표준: DICOM Print Service (물리적 크기 보장)
