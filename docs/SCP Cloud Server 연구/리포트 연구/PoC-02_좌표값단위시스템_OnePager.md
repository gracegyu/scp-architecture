Engineering One Pager

**Project Name**: PoC-02: 좌표값 단위 시스템 설계

**Date**: 2026년 1월 6일

**Submitter Info**: Raymond

**Project Description**: 기존 Desktop 제품에서 사용하던 정수 기반 좌표값 시스템을 실수 기반으로 전환하고, 다양한 화면 해상도와 용지 크기에 대응할 수 있는 통합 좌표 시스템을 설계합니다. E3 v5.1에서 비율값에서 mm 실측값으로 변경된 경험을 바탕으로, Cloud 환경에 최적화된 좌표 시스템을 구축합니다.

**Business and Marketing Justification**:

- **의료 정밀도 향상**: 정수값 한계로 인한 부정확한 측정값 문제 해결
- **멀티 디바이스 대응**: 다양한 해상도의 태블릿, 모바일에서 일관된 표시
- **프린터 호환성**: 고해상도 의료용 프린터의 정밀 출력 지원
- **사용자 경험 개선**: 확대/축소 시에도 정확한 측정값 유지
- **국제 표준 준수**: mm 기반 의료 측정 국제 표준과 일치
- **향후 확장성**: AI 기반 자동 측정 기능의 정밀도 기반 마련

**Risk Assessment**:

- **높은 리스크**:
  - 기존 비율 기반 데이터의 정확한 실측값 변환 복잡성
  - 다양한 디스플레이 DPI에서의 일관성 보장 어려움
- **중간 리스크**:
  - 부동소수점 연산 오차 누적 가능성
  - 기존 사용자의 측정값 기대치와 차이 발생 가능성
  - 프린터별 해상도 차이로 인한 출력 불일치
- **저위험**:
  - mm 단위는 의료계 표준으로 사용자 이해도 높음
- **완화 방안**:
  - 충분한 정밀도(소수점 3자리) 확보로 오차 최소화
  - 점진적 마이그레이션으로 사용자 적응 시간 제공

**Resource and Scheduling Details**:

- **기간**: 2주 (Week 1-2, PoC-01과 병렬 진행)
- **인력**:
  - Raymond (수학/알고리즘, Frontend 개발, QA 검증 역할 겸임)
    - 좌표 변환 로직 설계
    - 화면 렌더링 테스트
    - 정밀도 검증
- **도구**:
  - 다양한 해상도 테스트 기기 (모바일, 태블릿, 데스크톱)
  - 의료용 프린터 테스트 환경
  - 수학적 정밀도 검증 도구

**Technical Description**:

**현재 상황 분석**: 기존 시스템별 좌표 방식

- E2/E3 v4.0 이하: 0~1 범위 비율값 (용지 크기 대비)
- E3 v5.1 이상: mm 단위 실측값
- EzOrtho: Excel 기반 픽셀/포인트 단위
- 문제점: 정수 기반으로 인한 정밀도 한계, 해상도별 불일치

**새로운 좌표 시스템 설계**:

```typescript
interface Coordinate {
  x: number // mm 단위, 소수점 3자리 정밀도
  y: number // mm 단위, 소수점 3자리 정밀도
  unit: 'mm' // 단위 명시
}

interface ViewportInfo {
  dpi: number // 화면 DPI
  paperSize: PaperSize // 용지 크기 정보
  scaleFactor: number // 화면 확대/축소 비율
}
```

**변환 알고리즘**:

1. **비율값 → 실측값 변환**:

   ```
   실측값(mm) = 비율값 × 용지크기(mm) - 여백(mm)
   ```

2. **화면 좌표 변환**:

   ```
   픽셀좌표 = (실측값 × DPI) / 25.4
   ```

3. **정밀도 보장**:
   - 소수점 3자리 (0.001mm = 1μm) 정밀도
   - IEEE 754 부동소수점 오차 고려한 반올림

**테스트 시나리오**:

1. **변환 정확성 테스트**:

   - E2 샘플 파일 50개 변환 후 역변환 비교
   - E3 v4.0/v5.1 파일 변환 정확도 검증
   - EzOrtho 차트 데이터 변환 무결성 확인

2. **해상도별 일관성 테스트**:

   - 72 DPI (웹 표준) vs 300 DPI (인쇄 표준)
   - 모바일 (160-480 DPI) vs 태블릿 vs 데스크톱
   - 4K, 8K 고해상도 디스플레이 대응

3. **용지 크기별 테스트**:
   - A3, A4, Letter, 의료용 특수 용지 크기
   - Portrait vs Landscape 방향
   - 사용자 정의 용지 크기 지원

**성능 기준**:

- **정밀도**: 기존 정수값 대비 1000배 향상 (1mm → 0.001mm)
- **일관성**: 모든 해상도에서 ±0.1mm 이내 오차
- **성능**: 좌표 변환 시간 1ms 이내
- **호환성**: 100% 기존 데이터 변환 성공

**예상 결과물**:

1. **좌표 시스템 스펙 문서**:
   - 단위 정의, 정밀도 기준
   - 변환 공식 및 알고리즘
2. **변환 라이브러리**:
   - 양방향 변환 함수
   - 정밀도 검증 유틸리티
3. **테스트 결과 리포트**:
   - 해상도별 일관성 검증 결과
   - 성능 벤치마크 데이터
4. **마이그레이션 가이드**:
   - 기존 데이터 변환 절차
   - 주의사항 및 제한사항

**성공 시나리오**: mm 기반 실수 좌표 시스템으로 의료 이미지 정밀 측정 지원, 모든 디바이스에서 일관된 사용자 경험 제공

**실패 시 대안**: 하이브리드 방식 (화면: 비율값, 출력: 실측값) 또는 기존 비율값 시스템 유지
