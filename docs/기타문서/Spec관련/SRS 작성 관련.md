- 현재
    - SRS 작성 및 리뷰
        - VTS 이슈를 생성해서 진행
    - SRS 저장소
        - SVN (약 7~8년 이전에 사용, SRS는 doc) → baseline 원칙 적용
        - SharePoint > ES Project > (제품) > srs 에 보관 (약 7~8년 이후부터 사용, SRS 리뷰 편의를 위해서 편법 사용)
            - 이전 버전은 old 에 보관
        - VKS (최근, SRS 리뷰 편의를 위해서 편법 사용)
        - Azure DevOps (최근, MD 사용, baseline 원칙 다시 적용)
    - WBS (Excel, baseline에 포함 안됨)
        - SRS 작성이 완료될 때 보통 함께 완료된다.
        - 별도의 VTS 이슈 생성해서 진행
    - WBS 저장소
        - SharePoint/OneDrive 등 어딘가에 제품별로 관리 (PM들이 주로 관리했었음)
        - Online WBS (최근)
- TBD
    - 앞으로는 어떻게 할지 논의
        - 대원칙: 소스코드와 문서는 하나의 base 로 가야 한다.
        - Azure DevOps에 소스코드와 문서를 같은 repository에 둔다.
            - 제품이 여러 repo에 걸치는 경우는 그때그때 논의한다.
        - 숙제: DevOps, VKS 에 접근할 수 없는 사람도 스펙을 보거나 리뷰가능한 방안을 마련한다. 전규현/ Raymond
            - Azure DevOps: 개발자, PM 이 주로 권한이 있다.
                - Stakeholder가 보기만 가능한지 확인한다.
            - VKS: 각 계열사는 계열사의 Space만 볼 수 있게 되어 있고, 공통 Space도 있다.
            - SharePoint: 공유만 하면 어느 계열사도 볼 수 있다.
                - VN 외부사람은 안된다.
                - 외주 인력은 공유할 때만 가능하다.
            - OneDrive: VN 외부 사람까지 공유가 가능하다.

---

> **표준안 정리:** 위 논의를 바탕으로 한 저장·리뷰·참조 표준(안)은 별도 문서로 분리했습니다 → [`스펙문서 관리 표준 (저장·리뷰·참조).md`](./스펙문서%20관리%20표준%20(저장·리뷰·참조).md). (VKS 전사 표준 업무 프로세스 등록용)
