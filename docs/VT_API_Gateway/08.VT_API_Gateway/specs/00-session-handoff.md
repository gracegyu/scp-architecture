# 세션 핸드오프 — 이관됨 (redirect)

> **구현 인계·온보딩 정본은 `abc-dev-assistant/projects/vt-api-gateway/`로 이관되었습니다.**
>
> - **온보딩(진입점)**: `abc-dev-assistant/projects/vt-api-gateway/README.md`
> - **baseline 인계(핵심 결정·PR/미결 현황)**: `abc-dev-assistant/projects/vt-api-gateway/sessions/spec-baseline-handoff.md`
> - **IP**: `abc-dev-assistant/projects/vt-api-gateway/ImplementationPlan.md`
>
> 이 파일은 **이 스펙 세션(`5cf10750`·"VT-GW-스펙")의 resume 참고용**으로만 남깁니다. 상세 현황은 위 정본을 보세요.

## 이 스펙 세션 resume 메모 (비작업)

- 세션=`5cf10750`. 부모 `587af9a6`(온보딩 원본) 삭제로 **`claude -r` resume가 크래시**할 수 있음. **복귀=`claude` 실행 후 `←`(agent view)에서 "VT-GW-스펙" 선택**. 안 되면 새 세션에서 위 온보딩(`projects/vt-api-gateway/README.md`)으로 이어받기.
- 산출물은 전부 git/문서에 있어 무사.
