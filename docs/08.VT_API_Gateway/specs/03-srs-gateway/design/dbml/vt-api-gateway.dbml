// Vatech API Gateway - DBML (설계 초안)
//
// 작성: scp-architecture 초안 -> baseline 시 vt-api-gateway/docs/specs/design/로 PR 이관.
// 구현 착수 후에는 Prisma schema로 이어진다(본 파일이 컬럼·타입·인덱스·relation SSOT).
//
// 저장 정보 유형(§6.4): 디바이스 레지스트리, device/clinic<->region 매핑,
//   토큰 메타, 정책(OPA 입력), 감사 로그. PHI 본문 미저장(presigned 직결).

Project vt_api_gateway {
  database_type: 'PostgreSQL'
  Note: '설계 초안(placeholder). dev-chain-design 단계에서 테이블 확정 예정.'
}

// placeholder - baseline DBML 작성 시 제거
Table _placeholder {
  id int [pk, note: 'Remove when baseline DBML is written']
}
